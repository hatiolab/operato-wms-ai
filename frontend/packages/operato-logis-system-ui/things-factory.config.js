import route from './dist-client/route'
import bootstrap from './dist-client/bootstrap'

export default {
  route,
  routes: [
    {
      page: 'system-home',
      tagname: 'system-home'
    }
  ],
  bootstrap
}
