# Jasypt Encryption
jasypt.encryptor.password=${JASYPT_ENCRYPTOR_PASSWORD:operato-wms-secret}

# Shutdown endpoint disabled by default (enable only in dev profile)
management.endpoint.shutdown.enabled=false
