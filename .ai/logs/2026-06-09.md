# 2026-06-09 작업 로그

## 09:00 — manual-wave-create-popup.js 수동 웨이브 생성 팝업 신규 구현

### 작업 내용
- B2C(biz_type=B2C_OUT), wave_no 없음, REGISTERED/CONFIRMED 상태 주문을 표시하는 팝업 구현
- '출고 준비' 버튼: 주문별 `confirm_and_allocate` 순차 처리, 처리 결과를 '출고 준비 상태' 컬럼에 실시간 반영
- 성공 시 그리드 앞에 체크박스 표시
- oms-home.js의 '웨이브 생성' 버튼 클릭 시 이 팝업이 열리도록 연결

### 변경/생성 파일
- `frontend/packages/operato-wes/client/pages/oms/manual-wave-create-popup.js` — 신규 생성
- `frontend/packages/operato-wes/client/pages/oms/oms-home.js` — import 추가, `_openWaveNewPopup` 변경

### 주요 결정 또는 발견 사항
- `auto-wave-create-popup` 대신 `manual-wave-create-popup`을 '웨이브 생성' 버튼에 연결
- 팝업 size를 `medium` → `large`로 변경

---

## 09:30 — SHIPMENT_ORDER_STATUS 공통코드로 상태 표시

### 작업 내용
- `_statusLabel()` 하드코딩 제거
- `ServiceUtil.codeItems('SHIPMENT_ORDER_STATUS')` 로 동적 조회 후 `description` 표시
- `connectedCallback` 시 `_fetchStatusOptions()` 병렬 호출

### 변경/생성 파일
- `frontend/packages/operato-wes/client/pages/oms/manual-wave-create-popup.js` — statusOptions 상태 추가, `_fetchStatusOptions` 신규

---

## 10:00 — 웨이브 번호 표시 및 create_manual 선행 호출

### 작업 내용
- 상단 요약 바에 웨이브번호 표시 (초기 `-`, 출고 준비 후 실제 번호)
- '출고 준비' 버튼 클릭 시: ① `oms_trx/waves/create_manual` 호출 → waveNo 갱신 + 그리드 wave_no 업데이트 → ② 주문별 `confirm_and_allocate` 순차 처리

### 변경/생성 파일
- `frontend/packages/operato-wes/client/pages/oms/manual-wave-create-popup.js` — `waveNo` 상태 추가, `_startReadyProcess` 로직 변경

### 주요 결정 또는 발견 사항
- `create_manual` API: `{ orders: [{ id }], pick_type: 'INDIVIDUAL' }` → `{ wave_no, ... }` 반환

---

## 10:30 — '웨이브 확정' 버튼 추가

### 작업 내용
- action bar에 '웨이브 확정' 버튼 추가 (보라색 스타일)
- ALLOCATED 주문 0건: 경고 토스트
- ALLOCATED 주문 N건: 확인 팝업 → `POST oms_trx/waves/config_wave` 호출 → wave_no 갱신 + 그리드 업데이트

### 변경/생성 파일
- `frontend/packages/operato-wes/client/pages/oms/manual-wave-create-popup.js` — `_confirmWave` 메서드 추가, 버튼 렌더링 추가

### 주요 결정 또는 발견 사항
- `config_wave` API: `List<ShipmentOrder>` (배열 전달) → `{ wave_no, order_count, ... }` 반환

---

## 11:00 — 팝업 닫기 버그 수정

### 작업 내용
- `_close()` 에서 `customEvent('close-popup')` dispatch 방식 → `UiUtil.closePopupBy(this)` 로 수정
- `auto-wave-create-popup.js` 참조하여 `openPopup` 과 호환되는 닫기 방식 적용

### 변경/생성 파일
- `frontend/packages/operato-wes/client/pages/oms/manual-wave-create-popup.js` — `_close` 메서드 수정

### 주요 결정 또는 발견 사항
- `openPopup`으로 열린 팝업은 `UiUtil.closePopupBy(this)` 로만 닫힘 — `close-popup` 이벤트는 인식하지 않음

---

## 11:30 — 주문 조회 조건 개선 (최대 500건, created_at 정렬)

### 작업 내용
- 조회 limit을 200 → 500으로 변경
- 정렬 기준을 `ship_by_date, shipment_no` → `created_at` ASC (먼저 생성된 주문 우선)
- 상단 요약 바에 "웨이브 당 최대 500건" 뱃지 표시 추가

### 변경/생성 파일
- `frontend/packages/operato-wes/client/pages/oms/manual-wave-create-popup.js` — `_fetchOrders` 및 요약 바 수정

---

## 오늘 작업 요약

| 시각 | 작업 | 결과물 |
|------|------|--------|
| 09:00 | 수동 웨이브 생성 팝업 신규 구현 | manual-wave-create-popup.js, oms-home.js |
| 09:30 | SHIPMENT_ORDER_STATUS 공통코드 상태 표시 | manual-wave-create-popup.js |
| 10:00 | 웨이브번호 표시 + create_manual 선행 호출 | manual-wave-create-popup.js |
| 10:30 | 웨이브 확정 버튼 추가 (config_wave) | manual-wave-create-popup.js |
| 11:00 | 팝업 닫기 버그 수정 (UiUtil.closePopupBy) | manual-wave-create-popup.js |
| 11:30 | 주문 조회 limit 500 / created_at 정렬 / 상단 안내 표시 | manual-wave-create-popup.js |

---

## 13:00 — fulfillment-packing-pc.js 검수 수량 직접 입력 기능 추가

### 작업 내용
- 검수 항목 패널에 수량 직접 입력 필드 추가 (기존 스캔 방식 유지)
- 스캔할 때마다 +1 증가하되, 수량 많은 경우 직접 입력으로 처리 가능
- 포커스 시 기존 값 전체 선택 처리
- 미사용 변수 Hint 정리 (pageUpdated, pageDisposed, itemResults, forEach index)

### 변경/생성 파일
- `frontend/packages/operato-wes/client/pages/fulfillment/fulfillment-packing-pc.js` — qty 입력 필드 + `_onManualQtyInput` 추가, 미사용 변수 제거

### 주요 결정 또는 발견 사항
- 기존 CSS `.qty-row input` 스타일이 이미 있어 별도 CSS 추가 불필요
- `itemResults` API 호출 자체가 불필요했으므로 제거 (불필요한 N개 API 호출 제거 효과)

---

## 13:30 — pda-fulfillment-packing.js 포장 화면 다수 개선

### 작업 내용
1. **검수 수량 직접 입력**: qty-input-row 추가 (height 42px → 28px로 조정), 스캔 위/아래 위치 반복 수정
2. **완료 탭 자동 전환**: 항목 검수 완료 시 완료 탭으로 800ms 표시 후 대기 탭 복귀
3. **이전 완료 항목 탭 오류 수정**: 서버 반환 `PACKED`/`INSPECTED` 상태를 `COMPLETED`로 정규화 (로드 시 + `_confirmInspection`)
4. **바코드 스캔 후 포커스**: 스캔 완료/실패 후 바코드 입력으로 포커스 복귀
5. **포장 주문 카드**: B2C + invoice_no 있으면 송장번호 표시
6. **포장번호 검색**: `pack_order_no` + `shipment_no` + `invoice_no` 세 값 모두 매칭

### 변경/생성 파일
- `frontend/packages/operato-wes/client/pages/pda/pda-fulfillment-packing.js` — 다수 수정

### 주요 결정 또는 발견 사항
- 완료 탭 오류 원인: 탭 필터는 `COMPLETED` 체크, 서버는 `PACKED`/`INSPECTED` 반환 → 로드 시 정규화로 해결
- `_moveToNextItem`도 `status !== 'COMPLETED'` 기준이므로 PACKED 항목이 다음 항목으로 잘못 선택되던 문제 함께 해결

---

## 14:00 — packing-order-list.js 검수 수량 직접 입력 및 검색 개선

### 작업 내용
- 좌측 포장 목록 카드: B2C 송장번호 표시, 검색 조건 확장 (포장번호/출고번호/송장번호/고객명)
- placeholder 업데이트

### 변경/생성 파일
- `frontend/packages/operato-wes/client/pages/fulfillment/fulfillment-packing-pc.js` — 검색 및 카드 표시 수정

---

## 오늘 작업 요약 (추가분)

| 시각 | 작업 | 결과물 |
|------|------|--------|
| 13:00 | PC 포장 화면 검수 수량 직접 입력 | fulfillment-packing-pc.js |
| 13:30 | PDA 포장 화면 다수 개선 (수량입력/탭/포커스/검색/카드) | pda-fulfillment-packing.js |
| 14:00 | PC 포장 목록 검색 확장 + B2C 송장번호 표시 | fulfillment-packing-pc.js |
