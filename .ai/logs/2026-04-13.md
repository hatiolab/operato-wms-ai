# 2026-04-13 작업 로그

## 23:30 — 이미지 렌더러 getAttachment 함수 GraphQL → REST 전환

### 작업 내용
- `ox-grist-renderer-image-selector.ts`의 `getAttachment` 함수를 GraphQL 호출에서 백엔드 REST API 호출로 교체
- 기존 GraphQL 버전은 `getAttachmentByGraphql`로 이름 변경하여 참조용으로 유지 (export)
- 새 `getAttachment`: `GET /rest/attachments/{id}` → `path` 필드 → `/rest/storage/download?path=...` fullpath 구성

### 변경/생성 파일
- `frontend/packages/metapage/client/components/renderer/ox-grist-renderer-image-selector.ts` — getAttachment를 REST 기반으로 교체, getAttachmentByGraphql 참조용 유지

### 주요 결정 또는 발견 사항
- `getAttachmentByGraphql`은 사용하지 않아도 삭제하지 않고 `export`로 선언하여 lint 경고(6133) 회피
- 기존 GraphQL import(`gql`, `buildArgs`, `client`)는 `getAttachmentByGraphql` 유지를 위해 그대로 보존

---

## 23:45 — 이미지 에디터 저장값 스토리지 경로 → Attachment ID로 변경

### 작업 내용
- `ox-grist-editor-image-selector.ts`의 저장값을 스토리지 경로(`path`)에서 Attachment ID로 변경
- 셀렉터 팝업(`ox-storage-image-selector-popup.ts`)에서 `attachment_id`를 이벤트 detail에 포함하도록 수정
- 백엔드 `StorageBrowseItem`에 `attachmentId` 필드 추가, `enrichDescriptions()`에서 값 채움
- 썸네일 표시: `/rest/attachments/{id}/download` 엔드포인트 활용 (기존 AttachmentController에 존재 확인)
- 업로드 완료 시 `upload-complete` 이벤트의 `attachment_id` 필드를 저장값으로 사용

### 변경/생성 파일
- `otarepo-core/src/main/java/xyz/elidom/core/system/storage/StorageBrowseItem.java` — `attachmentId` 필드 + getter/setter 추가
- `otarepo-core/src/main/java/xyz/elidom/core/rest/StorageBrowseController.java` — `enrichDescriptions()`에서 `item.setAttachmentId(att.getId())` 추가
- `frontend/packages/metapage/client/components/popup/ox-storage-image-selector-popup.ts` — `ImageItem` 인터페이스에 `attachment_id` 추가, `_loadImages` 매핑 추가
- `frontend/packages/metapage/client/components/editor/ox-grist-editor-image-selector.ts` — 저장값을 `attachment_id`로 변경, 썸네일 URL을 `/rest/attachments/{id}/download`로 변경, JSDoc 업데이트

### 주요 결정 또는 발견 사항
- `GET /rest/attachments/{id}/download` 엔드포인트는 `AttachmentController`(otarepo-core)에 이미 구현되어 있음 — 별도 추가 불필요
- browse API 응답의 `attachment_id`(snake_case)는 Jackson SNAKE_CASE 전략에 의해 `attachmentId` Java 필드에서 자동 변환됨
- `_nameFromPath` 메서드는 `_onImageSelected`의 path 폴백으로 여전히 사용 중 — 제거하지 않음

---
## 오늘 작업 요약

| 시각 | 작업 | 결과물 |
|------|------|--------|
| 23:30 | 이미지 렌더러 getAttachment GraphQL → REST 전환 | `ox-grist-renderer-image-selector.ts` |
| 23:45 | 이미지 에디터 저장값 스토리지 경로 → Attachment ID 변경 | `StorageBrowseItem.java`, `StorageBrowseController.java`, `ox-storage-image-selector-popup.ts`, `ox-grist-editor-image-selector.ts` |
