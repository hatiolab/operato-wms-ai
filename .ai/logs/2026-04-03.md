# 2026-04-03 작업 로그

## 13:00 — 풀필먼트 검수 화면 바코드 스캔 자동 검수 수량 증가 기능 구현

### 작업 내용
- 검수/포장 PC 화면에서 바코드 스캔 시 작업자가 수량을 수동 입력하는 방식을 제거하고, 유효한 바코드 스캔 시마다 검수 수량이 자동으로 1씩 증가하도록 변경
- 주문 수량에 도달하면 자동으로 검수 완료 API 호출
- 이미 검수 수량이 주문 수량 이상인 항목은 추가 스캔 차단 및 경고 메시지 표시
- 수량 입력 필드를 읽기 전용 스캔 진행 상태 표시로 변경
- 검수 테이블의 수량 컬럼을 "검수/수량" (예: 3/5) 형식으로 변경
- `_autoConfirmIfSingleQty` 메서드 제거 (모든 수량에 동일한 자동 증가 로직 적용)

### 변경/생성 파일
- `frontend/packages/operato-wes/client/pages/fulfillment/fulfillment-packing-pc.js` — 바코드 스캔 시 자동 수량 증가, 수량 입력 UI 제거, 검수/수량 표시 형식 변경

### 주요 결정 또는 발견 사항
- 기존 `_autoConfirmIfSingleQty`는 수량 1인 경우만 자동 처리했으나, 새 로직은 모든 수량에 대해 스캔 기반 자동 증가로 통일

## 13:20 — 검수 완료 버튼 추가 및 건너뛰기 버튼 제거

### 작업 내용
- 바코드 스캔이 불가능한 상품을 위해 "검수 완료" 버튼 추가
- "건너뛰기" 버튼 제거 (검수 완료 버튼으로 대체 가능)
- `_skipCurrentItem` 메서드, `.btn-skip` CSS, F3 단축키 제거

### 변경/생성 파일
- `frontend/packages/operato-wes/client/pages/fulfillment/fulfillment-packing-pc.js` — 건너뛰기 기능 제거, 검수 완료 버튼 추가

### 주요 결정 또는 발견 사항
- 건너뛰기와 검수 완료 기능이 중복되어 혼란을 줄 수 있어 검수 완료 버튼 하나로 통합

## 13:35 — PackingOrder 상태 전이 (CREATED → IN_PROGRESS) 연동

### 작업 내용
- 프론트엔드에서 포장 주문 선택 시 백엔드 `POST ful_trx/packing_orders/{id}/start` API를 호출하여 CREATED → IN_PROGRESS 상태 전환
- 기존에는 API 호출 없이 프론트엔드만 검수 화면으로 전환되어 DB 상태가 CREATED인 채로 유지되는 문제 수정

### 변경/생성 파일
- `frontend/packages/operato-wes/client/pages/fulfillment/fulfillment-packing-pc.js` — `_onSelectOrder`에서 CREATED 상태 시 start API 호출 추가

### 주요 결정 또는 발견 사항
- 백엔드에 start API가 이미 존재했지만 프론트엔드에서 호출하지 않고 있었음

## 13:50 — 출고 확정 시 PackingOrderItem 상태 PACKED 변경 및 PackingBox 생성

### 작업 내용
- `finishPackingItem`은 원래대로 INSPECTED 상태까지만 처리 (검수 완료)
- `completePackingOrder`(출고 확정) 시 INSPECTED 아이템을 PACKED로 일괄 변경 (pack_qty = insp_qty)
- 프론트엔드에서 전달하는 박스 정보(boxType, boxCount, boxWeight, trackingNo)로 `packing_boxes` 레코드 생성
- Controller에 `@RequestBody` 파라미터 추가하여 박스 정보 수신
- 생성된 첫 번째 박스 ID를 모든 PACKED 아이템의 `packing_box_id`에 할당

### 변경/생성 파일
- `src/main/java/operato/wms/fulfillment/service/FulfillmentPackingService.java` — completePackingOrder에 박스 생성, 아이템 PACKED 전환, packing_box_id 할당 로직 추가
- `src/main/java/operato/wms/fulfillment/rest/FulfillmentTransactionController.java` — completePackingOrder 엔드포인트에 @RequestBody 파라미터 추가

### 주요 결정 또는 발견 사항
- 검수(INSPECTED)와 포장(PACKED)은 별도 단계로 유지: 검수 완료 → INSPECTED, 출고 확정 → PACKED
- 기존 Controller가 `@RequestBody`를 받지 않아 프론트엔드가 보내는 박스 정보가 무시되고 있었음
- 박스 생성 시 아이템 집계(총 품목수, 총 수량)를 첫 번째 박스에 기록, 중량은 박스 수로 균등 분배

## 14:30 — 출고 추적 (Shipment Tracking) 기능 구현

### 작업 내용
- 송장번호/출고번호/포장번호/피킹번호/웨이브번호/원주문번호 중 하나를 입력하면 주문→웨이브→피킹→포장→박스/출하 전체 이력을 조회하는 기능 신규 구현
- 백엔드: FulfillmentTrackingService (자동 감지, 순차 fallback, INDIVIDUAL/TOTAL 피킹 전략)
- 프론트엔드: 검색 섹션, 요약 카드 4개, 6단계 타임라인, 4개 탭 (주문/피킹/포장/박스·송장)
- 라우트 및 페이지 등록, 설계서 구현 현황 업데이트

### 변경/생성 파일
- `src/main/java/operato/wms/fulfillment/service/FulfillmentTrackingService.java` — 신규 생성 (~360줄), 출고 추적 서비스
- `src/main/java/operato/wms/fulfillment/rest/FulfillmentTransactionController.java` — GET /rest/ful_trx/tracking 엔드포인트 추가
- `frontend/packages/operato-wes/client/pages/fulfillment/shipment-tracking.js` — 신규 생성 (~650줄), 출고 추적 화면
- `frontend/packages/operato-wes/client/route.ts` — shipment-tracking import 추가
- `frontend/packages/operato-wes/things-factory.config.js` — shipment-tracking 라우트 등록
- `docs/design/fulfillment-screen-design.md` — 구현 현황 ✅ 업데이트

### 주요 결정 또는 발견 사항
- 자동 감지: 접두사(`W-`, `PICK-`, `PACK-`, `SH-`) 우선 → 순차 COUNT 쿼리 fallback (인덱스 컬럼)
- INDIVIDUAL vs TOTAL 피킹: shipment_order_id로 먼저 조회, 없으면 wave_no로 fallback
- Map 응답이므로 모든 키를 snake_case로 직접 작성 (Jackson 자동 변환 안 됨)

## 15:30 — packing-order-detail.js 매니페스트 버튼 화면 새로고침 수정

### 작업 내용
- 매니페스트 버튼 클릭 시 처리는 되지만 화면이 새로고침되지 않는 문제 수정
- `_refreshAfterAction()`가 `_fetchOrder()` 호출 시 `this.loading = true`로 설정하여 전체 팝업 콘텐츠가 로딩 div로 교체되는 문제
- loading 상태 없이 직접 데이터를 fetch하도록 변경

### 변경/생성 파일
- `frontend/packages/operato-wes/client/pages/fulfillment/packing-order-detail.js` — `_refreshAfterAction()` 메서드를 loading 없이 직접 fetch하도록 수정

### 주요 결정 또는 발견 사항
- 팝업 내에서 loading 상태 전환은 팝업 플리커를 유발하므로, 액션 후 새로고침 시에는 silent fetch 패턴 사용

## 15:50 — packing-order-detail.js 완료 버튼 에러 수정

### 작업 내용
- 완료 버튼 클릭 시 에러 발생 문제 수정
- `_completeOrder()`가 `ServiceUtil.restPost(url)` 호출 시 body를 전달하지 않아, 컨트롤러의 `@RequestBody` + `consumes = APPLICATION_JSON` 조건 불일치로 415/400 에러 발생
- 빈 객체 `{}`를 body로 전달하도록 수정

### 변경/생성 파일
- `frontend/packages/operato-wes/client/pages/fulfillment/packing-order-detail.js` — `_completeOrder()`에 `{}` body 추가

### 주요 결정 또는 발견 사항
- `completePackingOrder` 컨트롤러만 `consumes = APPLICATION_JSON` + `@RequestBody`를 요구 (다른 액션 엔드포인트는 body 불필요)
- 백엔드 서비스는 params null 시 기본값 사용 (boxCount=1, boxWeight=0.0)

## 16:00 — packing-order-detail.js 전체 액션 버튼 화면 새로고침 개선

### 작업 내용
- 라벨출력 버튼 등 모든 액션 버튼에서 화면이 새로고침되지 않는 문제 근본적 해결
- 7개 액션 메서드 모두에서 POST 응답을 `actionResult`로 캡처하여 `_refreshAfterAction(actionResult)`에 전달
- `_refreshAfterAction(actionResult)`: 먼저 응답의 status로 즉시 UI 갱신 후, GET 재조회로 전체 데이터 보강

### 변경/생성 파일
- `frontend/packages/operato-wes/client/pages/fulfillment/packing-order-detail.js` — 7개 액션 메서드 + `_refreshAfterAction` 개선

### 주요 결정 또는 발견 사항
- POST 응답에 이미 새 status가 포함되어 있으므로, GET 재조회 전에 즉시 UI 상태 반영
- GET 재조회가 실패하거나 stale 데이터를 반환해도 status는 이미 갱신됨 (안정성 확보)
- `operatoPost`에 body 없이 호출 시 `JSON.stringify(undefined)` → `undefined`가 전달되지만, `@RequestBody` 없는 엔드포인트는 정상 처리

---
## 오늘 작업 요약

| 시각 | 작업 | 결과물 |
|------|------|--------|
| 13:00 | 바코드 스캔 자동 검수 수량 증가 | fulfillment-packing-pc.js |
| 13:20 | 검수 완료 버튼 추가 / 건너뛰기 제거 | fulfillment-packing-pc.js |
| 13:35 | CREATED → IN_PROGRESS 상태 전이 연동 | fulfillment-packing-pc.js |
| 13:50 | 출고 확정 시 PACKED 전환 및 박스 생성 | FulfillmentPackingService.java, Controller |
| 14:30 | 출고 추적 기능 구현 (백엔드+프론트) | FulfillmentTrackingService.java, shipment-tracking.js 외 4개 |
| 15:30 | 매니페스트 버튼 새로고침 수정 | packing-order-detail.js |
| 15:50 | 완료 버튼 에러 수정 | packing-order-detail.js |
| 16:00 | 전체 액션 버튼 새로고침 개선 | packing-order-detail.js |
