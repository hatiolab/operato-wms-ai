# 2026-03-19 작업 로그

## 14:00 — CLAUDE.md 최적화 분석 및 방안 제안

### 작업 내용
- 현재 CLAUDE.md (5.4KB/108줄)를 분석하여 5가지 최적화 방안 도출
- 누락 섹션(행동 지침, 환경 정보, 테스트, 주요 파일 경로), 기존 섹션 경량화(빌드, Skills), 구조 개선 제안

### 주요 결정 또는 발견 사항
- CLAUDE.md는 매 대화 시작 시 자동 로드되므로, 행동 지침을 최상단에 배치하는 것이 가장 효과적
- `.claude/README.md`는 자동 로드되지 않아 참조 위임 효과가 제한적

---

## 14:10 — CLAUDE.md 행동 지침 섹션 추가

### 작업 내용
- 프로젝트 개요 바로 다음에 `## 행동 지침` 섹션 신규 추가
- 필수 규칙, 새 Entity 추가 절차, 새 용어/코드 추가, 하지 말 것, 참조 문서 5개 하위 섹션 구성
- `backend-dev-guide.md`, `commit.md`, `entity_meta_by_entity.md` 내용 기반으로 실제 프로젝트 규칙 반영

### 변경/생성 파일
- `CLAUDE.md` — `## 행동 지침` 섹션 추가 (필수 규칙, Entity 절차, 금지 사항, 참조 문서)

### 주요 결정 또는 발견 사항
- 사용자가 직접 수정: Controller/Service 작성하지 않음, `docs/design/database-specification.md` 업데이트로 변경
- 시스템 필드 관련 규칙은 사용자가 `entity_meta_by_entity.md`와 함께 조정

---

## 14:20 — CLAUDE.md 환경 정보 섹션 추가

### 작업 내용
- `## 환경 정보` 섹션 신규 추가 (DB 접속, 멀티테넌시, 주요 경로)
- 실제 파일 확인: `config.development.js`(프론트 DB), `application-dev.properties.template`(백엔드 DB)
- Skill에서 DB 직접 접속 시 참조 경로 명시

### 변경/생성 파일
- `CLAUDE.md` — `## 환경 정보` 섹션 추가 (DB 접속 3종, 멀티테넌시, 주요 경로 6개)

### 주요 결정 또는 발견 사항
- 사용자가 `DB 마이그레이션` 경로를 `정적 페이지 등록: things-factory.config.js`로 변경

---

## 14:30 — CLAUDE.md 빌드 섹션 축소 + Skills 테이블 간소화 + 테스트 안내 추가

### 작업 내용
- 빌드 섹션: 코드블록 3개(42줄) → 리스트형(13줄)으로 경량화, 테스트 명령 추가
- Skills 테이블: 3컬럼(명령/파일/설명) → 2컬럼(명령/설명)으로 간소화, `파일` 컬럼 제거
- `/entity_meta_by_entity`를 상단으로 이동 (가장 중요한 skill)

### 변경/생성 파일
- `CLAUDE.md` — 빌드 섹션 축소 (-29줄), Skills 테이블 간소화, 테스트 안내 추가

### 주요 결정 또는 발견 사항
- 전체 160줄 → 129줄 (-31줄), 정보 밀도 향상
- Skills 테이블의 `파일` 컬럼은 Claude가 이미 알고 있어 불필요

---

## 14:40 — 토큰 절감 추가 방안 분석

### 작업 내용
- 현재 토큰 절감 설정 분석 (autocompact 70%, DISABLE_NON_ESSENTIAL_MODEL_CALLS, .claudeignore)
- Skill 파일 크기 분석: `entity_meta_by_entity.md` 14KB가 최대
- `.claudeignore` 추가 제외, `permissions.allow` 확장, CLAUDE.md 추가 축소 3가지 방안 제안

### 주요 결정 또는 발견 사항
- Skill 전문은 해당 Skill 호출 시에만 로드됨 (매 대화가 아님)
- `.claude/README.md`는 자동 로드되지 않아 참조 위임 효과 제한적
- `bin/` 디렉토리에 1,169개 파일이 제외되지 않고 있음 발견

---

## 14:50 — .claudeignore 추가 제외 대상 등록

### 작업 내용
- `bin/` (Gradle bin 출력, 1,169개 파일), `frontend/packages/*/cache/`, `.vscode/` 추가
- 바이너리/폰트/이미지 확장자 9종 (`*.png`, `*.jpg`, `*.ttf` 등) 추가

### 변경/생성 파일
- `.claudeignore` — 4개 카테고리 추가 (bin, 캐시, IDE, 바이너리 확장자)

### 주요 결정 또는 발견 사항
- `bin/` 디렉토리만으로 1,169개 파일이 탐색 대상에서 제거됨

---

## 15:00 — permissions.allow 확장

### 작업 내용
- `python3 *`, `pip3 install *` 추가 (Skill DB 접속 시 매번 승인 프롬프트 제거)
- `cd frontend && yarn *`, `docker compose *`, `ls *`, `mkdir *`, `cp *`, `wc *` 추가

### 변경/생성 파일
- `.claude/settings.json` — `permissions.allow`에 8개 패턴 추가

### 주요 결정 또는 발견 사항
- `python3 *`은 `/entity_meta_by_entity`, `/translate`, `/add_terminology` 등에서 매번 승인이 필요했던 것을 해소
- `docker compose *` 추가 (기존 `docker-compose *`는 레거시 명령)

---

## 오늘 작업 요약

| 시각 | 작업 | 결과물 |
|------|------|--------|
| 14:00 | CLAUDE.md 최적화 분석 | 5가지 방안 도출 |
| 14:10 | 행동 지침 섹션 추가 | `CLAUDE.md` (필수 규칙, Entity 절차, 금지 사항) |
| 14:20 | 환경 정보 섹션 추가 | `CLAUDE.md` (DB 접속, 멀티테넌시, 주요 경로) |
| 14:30 | 빌드 축소 + Skills 간소화 + 테스트 추가 | `CLAUDE.md` (160줄 → 129줄) |
| 14:40 | 토큰 절감 추가 방안 분석 | 3가지 방안 제안 |
| 14:50 | .claudeignore 추가 제외 등록 | `.claudeignore` (+1,169개 파일 제외) |
| 15:00 | permissions.allow 확장 | `.claude/settings.json` (+8개 패턴) |
