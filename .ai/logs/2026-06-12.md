# 2026-06-12 작업 로그

## 09:00 — .classpath 자동 수정 스크립트 및 VSCode 태스크 설정

### 작업 내용
- Gradle 프로젝트 동기화(Refresh) 시 Buildship이 `.classpath`의 `src/main/resources` 항목에 `excluding="**"` 를 반복적으로 재추가하는 문제 근본 해결
- 5초마다 `.classpath`를 감시하여 `excluding="**"` 가 추가되면 자동으로 제거하는 스크립트 생성
- VSCode 폴더 오픈 시 해당 스크립트가 백그라운드에서 자동 실행되도록 태스크 구성

### 변경/생성 파일
- `scripts/fix-classpath.sh` — 신규 생성: `.classpath` 감시 및 자동 수정 스크립트
- `.vscode/tasks.json` — 신규 생성: 폴더 오픈 시 fix-classpath.sh 자동 실행 태스크

### 주요 결정 또는 발견 사항
- `fswatch` 미설치 환경이므로 `while true; sleep 5` 폴링 방식으로 구현
- VSCode `tasks.json`의 `"runOn": "folderOpen"` + `"isBackground": true` 조합으로 자동 실행
- 최초 1회 VSCode에서 "자동 실행 태스크 허용" 승인 필요
- Gradle 동기화 후 5초 이내 자동 수정되므로 이후 `/fix_classpath` 수동 실행 불필요

---
## 오늘 작업 요약

| 시각 | 작업 | 결과물 |
|------|------|--------|
| 09:00 | .classpath 자동 수정 스크립트 및 VSCode 태스크 설정 | scripts/fix-classpath.sh, .vscode/tasks.json |
