# 2026-03-20 작업 로그

## 09:00 — VasResult 엔티티에 vasNo 필드 추가

### 작업 내용
- `VasResult` 엔티티에 `vasNo` 필드 추가 (`@Column(name = "vas_no", length = 30)`)
- VasResultController의 index 함수를 JOIN으로 수정하는 방안 → 사용자 요청으로 롤백
- 대신 엔티티 필드로 직접 추가하는 단순한 방식 채택

### 변경/생성 파일
- `src/main/java/operato/wms/vas/entity/VasResult.java` — `vasNo` 필드, getter/setter 추가

### 주요 결정 또는 발견 사항
- Controller JOIN 방식보다 엔티티 필드 추가가 기존 `this.search()` 패턴과 호환되어 더 단순

---

## 09:10 — vas_results.vas_no 데이터 일괄 업데이트

### 작업 내용
- `vas_results` 테이블의 빈 `vas_no` 컬럼을 `vas_orders` 테이블과 JOIN하여 일괄 업데이트
- Python/psycopg2로 UPDATE 실행, 12건 업데이트 완료

### 주요 결정 또는 발견 사항
- DB 접속: `config.development.js`의 ormconfig 참조 (localhost:15432/britestone_3pl_dev)

---

## 09:20 — ValueUtil 날짜 포맷 유틸리티 함수 추가

### 작업 내용
- `todayFormatted(format)` — 오늘 날짜를 지정 포맷으로 반환
- `formatDate(date, format)` — 입력 날짜(Date/String/Number)를 지정 포맷으로 반환
- `todayFormatted`는 `formatDate`에 위임하는 구조로 리팩토링

### 변경/생성 파일
- `frontend/packages/metapage/client/utils/value-util.js` — `todayFormatted()`, `formatDate()` 2개 메소드 추가

### 주요 결정 또는 발견 사항
- YYYY/MM/DD/HH/mm/ss 토큰 지원, 유효하지 않은 날짜는 빈 문자열 반환

---

## 09:30 — VAS 신규 주문 팝업 — 화주사/창고 콤보 박스 변환

### 작업 내용
- `vas-order-new-popup.js`에서 화주사, 창고 필드를 텍스트 입력에서 select 콤보 박스로 변경
- REST API: `companies` (com_cd, com_nm), `warehouses` (wh_cd, wh_nm)
- `_fetchCompanies()`, `_fetchWarehouses()` 데이터 조회 메소드 추가

### 변경/생성 파일
- `frontend/packages/operato-wes/client/pages/vas/vas-order-new-popup.js` — companies/warehouses 속성, fetch 메소드, select HTML 추가

---

## 09:40 — VAS 신규 주문 팝업 — 작업장 로케이션 콤보 박스 추가

### 작업 내용
- 작업장 로케이션 필드도 select 콤보 박스로 변경
- REST API: `locations` (loc_cd, loc_type)
- `loc_type = 'VAS'` 필터 적용하여 VAS 전용 로케이션만 조회

### 변경/생성 파일
- `frontend/packages/operato-wes/client/pages/vas/vas-order-new-popup.js` — locations 속성, `_fetchLocations()` 메소드 (VAS 필터), select HTML 추가

### 주요 결정 또는 발견 사항
- `searchByPagination('locations', [{ name: 'loc_type', value: 'VAS' }])` 패턴으로 필터 적용

---

## 09:50 — VAS 작업 화면 헤더 제거 및 새로고침 버튼 위치 변경

### 작업 내용
- `vas-work-page.js`에서 `.pda-header` 헤더 영역 제거 (주문 선택 화면, 작업 화면 모두)
- 새로고침 버튼을 검색 버튼 옆에 나란히 배치

### 변경/생성 파일
- `frontend/packages/operato-wes/client/pages/work/vas-work-page.js` — `_renderOrderSelect()`, `_renderWorkScreen()` 헤더 제거, 새로고침 버튼 재배치

### 주요 결정 또는 발견 사항
- 작업 화면의 뒤로가기는 하단 `취소` 버튼으로 대체 가능하여 헤더 불필요

---

## 10:00 — VasResult workedAt → workDate 필드 타입 변경

### 작업 내용
- `VasResult` 엔티티의 `workedAt` (java.util.Date) 필드를 `workDate` (String, 10자리 YYYY-MM-DD)로 변경
- 컬럼명 `worked_at` → `work_date`, 인덱스 `ix_vas_results_4` 업데이트
- `beforeCreate()`의 기본값 로직을 `new java.util.Date()` → `DateUtil.todayStr("yyyy-MM-dd")`로 변경
- `docs/design/vas-table-design.md` 문서도 5곳 동기화 수정

### 변경/생성 파일
- `src/main/java/operato/wms/vas/entity/VasResult.java` — 필드 타입/이름/인덱스/getter/setter/beforeCreate 전면 수정
- `docs/design/vas-table-design.md` — 필드명, 타입, 인덱스 DDL, 라이프사이클 설명 업데이트

### 주요 결정 또는 발견 사항
- Date 타입보다 String(10) `YYYY-MM-DD` 형식이 일자만 필요한 필드에 더 적합
- `DateUtil.todayStr("yyyy-MM-dd")` 유틸 활용

---

## 10:10 — work_date 용어 등록

### 작업 내용
- `/add_terminology label work_date 작업일자` 실행
- 5개 도메인 × 4개 언어(ko/en/ja/zh) = 20건 INSERT
- ko: 작업일자, en: Work Date, ja: 作業日, zh: 工作日期

### 주요 결정 또는 발견 사항
- 모든 도메인에 일괄 등록하여 멀티테넌시 환경에서 누락 없음

---

## 10:15 — 프론트엔드 번역 캐시 초기화

### 작업 내용
- `/clear_frontend_cache` 실행
- `frontend/packages/operato-wes/cache/translations/` 하위 캐시 파일 삭제
- 새 용어(work_date) 등록 후 프론트엔드에 반영하기 위해 캐시 초기화

---

## 10:30 — 입고 대시보드 설계 및 문서화

### 작업 내용
- VAS 대시보드(`vas-home.js`)와 유사한 입고 관리 대시보드 화면 설계
- 입고 상태별 현황 카드 (작성중/대기/작업중/완료)
- 입고 유형별 차트 (일반/반품/기타)
- 검수 현황 통계 (대기/완료/불량)
- 알림 영역 및 바로가기 버튼 구성

### 변경/생성 파일
- `docs/design/gi-screen-design.md` — 입고 대시보드 설계 문서 (나중에 inbound-screen-design.md로 변경됨)

### 주요 결정 또는 발견 사항
- VAS 대시보드 패턴을 입고 프로세스에 맞게 조정
- 입고 상태: INWORK/READY/START/END (WmsInboundConstants 참조)
- 검수 상태: PASS/FAIL (WmsInboundConstants 참조)

---

## 11:00 — 입고 대시보드 백엔드 API 구현

### 작업 내용
- 입고 대시보드용 4개 REST API 엔드포인트 추가
- `/rest/inbound_trx/dashboard/status-counts` — 상태별 건수 조회
- `/rest/inbound_trx/dashboard/type-stats` — 유형별 통계
- `/rest/inbound_trx/dashboard/inspection-stats` — 검수 통계
- `/rest/inbound_trx/dashboard/alerts` — 알림 목록
- SQL 기반 집계 쿼리 구현 (domain_id 필터 적용)

### 변경/생성 파일
- `src/main/java/operato/wms/inbound/rest/InboundTransactionController.java` — 4개 대시보드 엔드포인트 추가
- `src/main/java/operato/wms/inbound/service/InboundTransactionService.java` — 4개 대시보드 메소드 구현

### 주요 결정 또는 발견 사항
- `@GetMapping` + snake_case 파라미터 (`com_cd`, `wh_cd`, `target_date`)
- 멀티테넌시 지원: 모든 쿼리에 `domain_id` 조건 필수
- 날짜 기본값: `DateUtil.todayStr()` 사용

---

## 11:30 — 백엔드 빌드

### 작업 내용
- `/build` skill 실행 (기본 빌드, 테스트 포함)
- Gradle 빌드 성공 확인
- JAR 파일 생성 확인

### 주요 결정 또는 발견 사항
- 컴파일 오류 없이 빌드 완료
- 대시보드 API가 정상적으로 컴파일됨

---

## 11:40 — 입고 대시보드 프론트엔드 구현

### 작업 내용
- `inbound-home.js` 전면 재작성
- Material Design 3 스타일 적용
- Chart.js 통합 (유형별 막대 차트)
- 4개 API 호출 로직 구현 (`_fetchStatusCounts`, `_fetchTypeStats`, `_fetchInspectionStats`, `_fetchAlerts`)
- 상태 카드 클릭 시 필터 적용하여 receivings 페이지 이동
- 바로가기 버튼 (입고 지시, 입고 검수, 입고 실적, 재고 조회)

### 변경/생성 파일
- `frontend/packages/operato-wes/client/pages/inbound/inbound-home.js` — 완전히 새로 작성 (530줄)

### 주요 결정 또는 발견 사항
- `ServiceUtil.restGet()` 패턴으로 백엔드 REST API 호출
- Chart.js `bar` 타입, `borderRadius: 8`로 둥근 모서리
- `pageUpdated()` 라이프사이클에서 데이터 로딩
- `pageDisposed()`에서 Chart 인스턴스 정리

---

## 12:30 — 입고 대시보드 용어 등록

### 작업 내용
- `/add_terminology menu ReceivingDashboard 입고 대시보드` 실행
- 5개 도메인 × 4개 언어(ko/en/ja/zh) = 20건 INSERT
- ko: 입고 대시보드, en: Receiving Dashboard, ja: 入庫ダッシュボード, zh: 入库仪表板

---

## 12:35 — 번역 캐시 초기화

### 작업 내용
- `/clear_frontend_cache` 실행
- `frontend/packages/operato-wes/cache/translations/` 하위 캐시 파일 삭제
- 새 용어(ReceivingDashboard) 등록 후 프론트엔드에 반영하기 위해 캐시 초기화

---

## 12:40 — 입고 설계 문서 파일명 변경

### 작업 내용
- `docs/design/gi-screen-design.md` → `docs/design/inbound-screen-design.md`로 파일명 변경
- 더 명확하고 일관된 네이밍 컨벤션 적용 (gi → inbound)

### 변경/생성 파일
- `docs/design/inbound-screen-design.md` — 파일명 변경

---

## 13:00 — 출고 대시보드 설계 및 문서화

### 작업 내용
- 입고 대시보드와 유사한 패턴으로 출고 관리 대시보드 설계
- 출고 상태별 현황 카드 (REG/READY/RUN/END)
- 출고 유형별 차트 (NORMAL/RETURN/TRANSFER/SCRAP/ETC)
- 피킹 통계 (WAIT/PICKING/PICKED)
- 사업 유형 통계 (B2B/B2C)
- 6개 API 엔드포인트 사양 정의
- 프론트엔드 컴포넌트 구조 설계

### 변경/생성 파일
- `docs/design/outbound-screen-design.md` — 출고 대시보드 설계 문서 (완전한 API 스펙 + 프론트엔드 구조 포함)

### 주요 결정 또는 발견 사항
- ReleaseOrder 엔티티 분석: 8개 상태 코드 (REG/REQ/WAIT/READY/RUN/PICKED/END/CANCEL)
- 출고 유형: NORMAL/RETURN/TRANSFER/SCRAP/ETC (WmsOutboundConstants 참조)
- 피킹 프로세스 반영: WAIT → PICKING → PICKED 단계
- 입고/출고 비교표 작성으로 차이점 명확화

---

## 13:30 — 출고 대시보드 백엔드 API 구현

### 작업 내용
- 출고 대시보드용 5개 REST API 엔드포인트 추가
- `/rest/outbound_trx/dashboard/status-counts` — 상태별 건수 조회 (REG/READY/RUN/END)
- `/rest/outbound_trx/dashboard/type-stats` — 유형별 통계 (NORMAL/RETURN/TRANSFER/SCRAP/ETC)
- `/rest/outbound_trx/dashboard/picking-stats` — 피킹 통계 (WAIT/RUN/END)
- `/rest/outbound_trx/dashboard/business-type-stats` — 사업 유형 통계 (B2B/B2C)
- `/rest/outbound_trx/dashboard/alerts` — 알림 목록 (지연 출고, 피킹 대기)
- SQL 기반 집계 쿼리 구현 (domain_id 필터 적용)

### 변경/생성 파일
- `src/main/java/operato/wms/outbound/rest/OutboundTransactionController.java` — 5개 대시보드 엔드포인트 추가
- `src/main/java/operato/wms/outbound/service/OutboundTransactionService.java` — 5개 대시보드 메소드 구현 (305줄 추가)

### 주요 결정 또는 발견 사항
- `@RequestMapping` + snake_case 파라미터 (`com_cd`, `wh_cd`, `target_date`, `start_date`, `end_date`)
- 멀티테넌시 지원: 모든 쿼리에 `domain_id` 조건 필수
- 사업 유형 변환 로직: `B2B_OUT` → `B2B`, `B2C_OUT` → `B2C`
- 알림 조회: 지연 출고 (rls_ord_date < today) + 피킹 대기 (status = WAIT)

---

## 14:00 — 출고 대시보드 프론트엔드 구현

### 작업 내용
- `outbound-home.js` 전면 재작성
- Material Design 3 스타일 적용
- Chart.js 통합 (유형별 막대 차트 — 5개 타입)
- 5개 API 호출 로직 구현 (`_fetchStatusCounts`, `_fetchTypeStats`, `_fetchPickingStats`, `_fetchBizTypeStats`, `_fetchAlerts`)
- 상태 카드 클릭 시 필터 적용하여 releases 페이지 이동
- 바로가기 버튼 (출고 지시, 피킹 작업, 출고 실적, 재고 조회)

### 변경/생성 파일
- `frontend/packages/operato-wes/client/pages/outbound/outbound-home.js` — 완전히 새로 작성 (580+ 줄)

### 주요 결정 또는 발견 사항
- `ServiceUtil.restGet()` 패턴으로 백엔드 REST API 호출
- Chart.js `bar` 타입, 5개 출고 유형 시각화 (NORMAL/RETURN/TRANSFER/SCRAP/ETC)
- 입고 대시보드와 유사한 구조이지만 피킹 통계와 사업 유형 통계 추가
- `pageUpdated()` 라이프사이클에서 데이터 로딩
- `pageDisposed()`에서 Chart 인스턴스 정리

---

## 14:30 — 출고 대시보드 용어 등록

### 작업 내용
- `/add_terminology menu OutboundDashboard 출고 대시보드` 실행
- 5개 도메인 × 4개 언어(ko/en/ja/zh) = 20건 INSERT
- ko: 출고 대시보드, en: Outbound Dashboard, ja: 出庫ダッシュボード, zh: 出库仪表板
- 기존 ShippingDashboard 20건 삭제 후 OutboundDashboard로 재등록

### 주요 결정 또는 발견 사항
- 입고/출고 일관성: InboundDashboard / OutboundDashboard 네이밍 통일

---

## 14:35 — 번역 캐시 초기화

### 작업 내용
- `/clear_frontend_cache` 실행
- `frontend/packages/operato-wes/cache/translations/` 하위 캐시 파일 삭제
- 새 용어(OutboundDashboard) 등록 후 프론트엔드에 반영하기 위해 캐시 초기화

---

## 14:40 — 입고 대시보드 용어 수정

### 작업 내용
- terminologies 테이블에서 `ReceivingDashboard` → `InboundDashboard`로 수정
- 5개 도메인 × 4개 언어 = 20건 UPDATE
- ko: 입고 대시보드, en: Inbound Dashboard, ja: 入庫ダッシュボード, zh: 入库仪表板

### 주요 결정 또는 발견 사항
- 입고/출고 네이밍 일관성 확보: Inbound/Outbound 용어로 통일
- Receiving/Shipping 대신 Inbound/Outbound 사용

---

## 14:45 — 번역 캐시 재초기화

### 작업 내용
- `/clear_frontend_cache` 실행
- `frontend/packages/operato-wes/cache/translations/` 하위 캐시 파일 삭제
- 수정된 용어(InboundDashboard) 반영을 위해 캐시 초기화

---

## 오늘 작업 요약

| 시각 | 작업 | 결과물 |
|------|------|--------|
| 09:00 | VasResult vasNo 필드 추가 | `VasResult.java` (필드 + getter/setter) |
| 09:10 | vas_results.vas_no 데이터 업데이트 | DB 12건 UPDATE |
| 09:20 | ValueUtil 날짜 포맷 함수 추가 | `value-util.js` (todayFormatted, formatDate) |
| 09:30 | 화주사/창고 콤보 박스 변환 | `vas-order-new-popup.js` (select + API 조회) |
| 09:40 | 로케이션 콤보 박스 + VAS 필터 | `vas-order-new-popup.js` (loc_type=VAS 필터) |
| 09:50 | VAS 작업 화면 헤더 제거 | `vas-work-page.js` (헤더 제거, 버튼 재배치) |
| 10:00 | VasResult workedAt → workDate 변경 | `VasResult.java` + `vas-table-design.md` |
| 10:10 | work_date 용어 등록 | terminologies 20건 INSERT |
| 10:15 | 번역 캐시 초기화 | translations 캐시 삭제 |
| 10:30 | 입고 대시보드 설계 | `gi-screen-design.md` (설계 문서) |
| 11:00 | 입고 대시보드 백엔드 API 구현 | `InboundTransactionController.java` + `InboundTransactionService.java` |
| 11:30 | 백엔드 빌드 | Gradle 빌드 성공 |
| 11:40 | 입고 대시보드 프론트엔드 구현 | `inbound-home.js` (530줄, Chart.js 통합) |
| 12:30 | 입고 대시보드 용어 등록 | terminologies 20건 INSERT |
| 12:35 | 번역 캐시 초기화 | translations 캐시 삭제 |
| 12:40 | 설계 문서 파일명 변경 | `inbound-screen-design.md` |
| 13:00 | 출고 대시보드 설계 | `outbound-screen-design.md` (설계 문서) |
| 13:30 | 출고 대시보드 백엔드 API 구현 | `OutboundTransactionController.java` + `OutboundTransactionService.java` (305줄) |
| 14:00 | 출고 대시보드 프론트엔드 구현 | `outbound-home.js` (580+ 줄, Chart.js 통합) |
| 14:30 | 출고 대시보드 용어 등록 | terminologies 20건 INSERT (OutboundDashboard) |
| 14:35 | 번역 캐시 초기화 | translations 캐시 삭제 |
| 14:40 | 입고 대시보드 용어 수정 | terminologies 20건 UPDATE (ReceivingDashboard → InboundDashboard) |
| 14:45 | 번역 캐시 재초기화 | translations 캐시 삭제 |
