# 2026-03-20 작업 로그

## 09:00 — VasResult 엔티티에 vasNo 필드 추가

### 작업 내용
- `VasResult` 엔티티에 `vasNo` 필드 추가 (`@Column(name = "vas_no", length = 30)`)
- VasResultController의 index 함수를 JOIN으로 수정하는 방안 → 사용자 요청으로 롤백
- 대신 엔티티 필드로 직접 추가하는 단순한 방식 채택

### 변경/생성 파일
- `src/main/java/operato/wms/vas/entity/VasResult.java` — `vasNo` 필드, getter/setter 추가

### 주요 결정 또는 발견 사항
- Controller JOIN 방식보다 엔티티 필드 추가가 기존 `this.search()` 패턴과 호환되어 더 단순

---

## 09:10 — vas_results.vas_no 데이터 일괄 업데이트

### 작업 내용
- `vas_results` 테이블의 빈 `vas_no` 컬럼을 `vas_orders` 테이블과 JOIN하여 일괄 업데이트
- Python/psycopg2로 UPDATE 실행, 12건 업데이트 완료

### 주요 결정 또는 발견 사항
- DB 접속: `config.development.js`의 ormconfig 참조 (localhost:15432/britestone_3pl_dev)

---

## 09:20 — ValueUtil 날짜 포맷 유틸리티 함수 추가

### 작업 내용
- `todayFormatted(format)` — 오늘 날짜를 지정 포맷으로 반환
- `formatDate(date, format)` — 입력 날짜(Date/String/Number)를 지정 포맷으로 반환
- `todayFormatted`는 `formatDate`에 위임하는 구조로 리팩토링

### 변경/생성 파일
- `frontend/packages/metapage/client/utils/value-util.js` — `todayFormatted()`, `formatDate()` 2개 메소드 추가

### 주요 결정 또는 발견 사항
- YYYY/MM/DD/HH/mm/ss 토큰 지원, 유효하지 않은 날짜는 빈 문자열 반환

---

## 09:30 — VAS 신규 주문 팝업 — 화주사/창고 콤보 박스 변환

### 작업 내용
- `vas-order-new-popup.js`에서 화주사, 창고 필드를 텍스트 입력에서 select 콤보 박스로 변경
- REST API: `companies` (com_cd, com_nm), `warehouses` (wh_cd, wh_nm)
- `_fetchCompanies()`, `_fetchWarehouses()` 데이터 조회 메소드 추가

### 변경/생성 파일
- `frontend/packages/operato-wes/client/pages/vas/vas-order-new-popup.js` — companies/warehouses 속성, fetch 메소드, select HTML 추가

---

## 09:40 — VAS 신규 주문 팝업 — 작업장 로케이션 콤보 박스 추가

### 작업 내용
- 작업장 로케이션 필드도 select 콤보 박스로 변경
- REST API: `locations` (loc_cd, loc_type)
- `loc_type = 'VAS'` 필터 적용하여 VAS 전용 로케이션만 조회

### 변경/생성 파일
- `frontend/packages/operato-wes/client/pages/vas/vas-order-new-popup.js` — locations 속성, `_fetchLocations()` 메소드 (VAS 필터), select HTML 추가

### 주요 결정 또는 발견 사항
- `searchByPagination('locations', [{ name: 'loc_type', value: 'VAS' }])` 패턴으로 필터 적용

---

## 09:50 — VAS 작업 화면 헤더 제거 및 새로고침 버튼 위치 변경

### 작업 내용
- `vas-work-page.js`에서 `.pda-header` 헤더 영역 제거 (주문 선택 화면, 작업 화면 모두)
- 새로고침 버튼을 검색 버튼 옆에 나란히 배치

### 변경/생성 파일
- `frontend/packages/operato-wes/client/pages/work/vas-work-page.js` — `_renderOrderSelect()`, `_renderWorkScreen()` 헤더 제거, 새로고침 버튼 재배치

### 주요 결정 또는 발견 사항
- 작업 화면의 뒤로가기는 하단 `취소` 버튼으로 대체 가능하여 헤더 불필요

---

## 오늘 작업 요약

| 시각 | 작업 | 결과물 |
|------|------|--------|
| 09:00 | VasResult vasNo 필드 추가 | `VasResult.java` (필드 + getter/setter) |
| 09:10 | vas_results.vas_no 데이터 업데이트 | DB 12건 UPDATE |
| 09:20 | ValueUtil 날짜 포맷 함수 추가 | `value-util.js` (todayFormatted, formatDate) |
| 09:30 | 화주사/창고 콤보 박스 변환 | `vas-order-new-popup.js` (select + API 조회) |
| 09:40 | 로케이션 콤보 박스 + VAS 필터 | `vas-order-new-popup.js` (loc_type=VAS 필터) |
| 09:50 | VAS 작업 화면 헤더 제거 | `vas-work-page.js` (헤더 제거, 버튼 재배치) |
