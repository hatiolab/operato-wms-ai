# 2026-05-22 작업 로그

## 09:00 — `inventory_transactions` 설계 문서 정리 (tran_type, direction 컬럼)

### 작업 내용
- `tran_type` 코드 목록에서 `IN_INSP` (입고 검수) 제거
- `inventory_transactions` 테이블에 `direction VARCHAR(3)` 컬럼 추가 (IN: 수량 증가, OUT: 수량 감소)

### 변경/생성 파일
- `docs/design/stock-transaction-table-design.md` — `IN_INSP` 행 삭제 (1.3, 2.2, 2.5 세 곳), `direction` 컬럼 추가 (1.2)

### 주요 결정 또는 발견 사항
- `IN_INSP` 제거는 1.3 tran_type 목록 외에 2.2 `in_qty` 설명, 2.5 배치 SQL 세 곳에 걸쳐 있어 함께 수정
- `direction` 컬럼은 `tran_qty` 부호와 별개로 수불 방향을 명시적으로 표현하는 용도

---

## 09:30 — `Inventory` 엔티티 컬럼 4개 추가

### 작업 내용
- `received_at VARCHAR(20)`, `closed_at VARCHAR(20)`, `unit_price DECIMAL(15,4)`, `currency_cd VARCHAR(3)` 컬럼 추가
- `import java.math.BigDecimal` 추가, 중복 import 정리

### 변경/생성 파일
- `src/main/java/operato/wms/stock/entity/Inventory.java` — 4개 필드 및 getter/setter 추가

### 주요 결정 또는 발견 사항
- `received_at`, `closed_at`은 기존 날짜 필드(`expired_date` 등)와 동일하게 `String` 타입 사용 (VARCHAR 20)
- `unit_price`는 금액 정밀도를 위해 `BigDecimal` 사용 (Java `Double`은 PostgreSQL `FLOAT8`로 매핑되어 부동소수점 오차 위험)
- `received_at` 배치: `rcv_seq` 바로 뒤, `unit_price`/`currency_cd` 배치: `cbm` 바로 뒤

---

## 10:00 — `stock-inventory-improvement.md` 완료 항목 업데이트

### 작업 내용
- Inventory 엔티티에 추가된 컬럼들을 문서에 반영
- P1-3, P1-4, P1-5, P1-6, P1-7 문제점 해결 표시
- TASK-07, TASK-08, TASK-13 완료 표시

### 변경/생성 파일
- `docs/design/stock-inventory-improvement.md` — 1.1 현행 구조 신규 컬럼 추가, P1-3~P1-7 해결 표시, TASK-07/08/13 완료 표시, 로드맵 완료 표시

### 주요 결정 또는 발견 사항
- P1-5 (`del_flag` 소진 처리)는 `closed_at` 추가로 부분 해결 — `del_flag` 이중 관리 구조 주의사항 추가
- TASK-07은 문서상 `rcv_confirmed_at` → 실제 구현은 `received_at` 이름으로 적용

---
## 오늘 작업 요약

| 시각 | 작업 | 결과물 |
|------|------|--------|
| 09:00 | inventory_transactions tran_type IN_INSP 제거 + direction 컬럼 추가 | `docs/design/stock-transaction-table-design.md` |
| 09:30 | Inventory 엔티티 컬럼 4개 추가 (received_at, closed_at, unit_price, currency_cd) | `src/main/java/operato/wms/stock/entity/Inventory.java` |
| 10:00 | stock-inventory-improvement.md 완료 항목 업데이트 (P1-3~7, TASK-07/08/13) | `docs/design/stock-inventory-improvement.md` |
