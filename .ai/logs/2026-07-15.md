# 2026-07-15 작업 로그

## 10:00 — BACK_ORDER 재할당 팝업 생성

### 작업 내용
- `manual-wave-create-popup.js`와 동일한 구조로 BACK_ORDER(재고 부족) 상태 주문을 조회하여 재고 재할당 처리하는 팝업 화면 생성
- 재할당 API: `POST /rest/oms_trx/shipment_orders/{id}/confirm_and_allocate` (BACK_ORDER → ALLOCATED 또는 재고 여전히 부족 시 BACK_ORDER 유지)
- 메모리 기반 페이지네이션(100건/page), 필터 칩(전체/완료/재고부족/오류/대기), 진행바, 검색바 포함
- 초기 조회 완료 시 전체 선택, 일시 중지/재개, 경과 시간 타이머 기능 포함

### 변경/생성 파일
- `frontend/packages/operato-wes/client/pages/oms/backorder-reallocate-popup.js` — 신규 생성 (BACK_ORDER 재할당 팝업)
- `frontend/packages/operato-wes/client/route.ts` — `backorder-reallocate-popup` import 추가

### 주요 결정 또는 발견 사항
- `confirm_and_allocate` API가 BACK_ORDER 상태에서도 동작함을 확인 (`OmsShipmentOrderService`에서 CONFIRMED || BACK_ORDER 조건 처리)
- `back_order: true` 응답이면 재고 여전히 부족(BACK_ORDER), `false`이면 재할당 성공(ALLOCATED)
- 팝업 호출: `UiUtil.openDynamicPopup('backorder-reallocate-popup', '백오더 재할당', {})`

---

## 11:00 — StoragePolicy 엔티티 b2b_insp_flag 컬럼 추가

### 작업 내용
- `StoragePolicy` 엔티티에 B2B 출고 검수 여부 컬럼(`b2b_insp_flag`) 추가
- Java 엔티티 필드, DB 테이블 컬럼, entity_columns, menu_columns 모두 추가

### 변경/생성 파일
- `src/main/java/operato/wms/base/entity/StoragePolicy.java` — `b2bInspFlag` 필드 + getter/setter 추가 (`b2bWaitShipLoc` 앞에 배치)

### 주요 결정 또는 발견 사항
- DB 컬럼: `ALTER TABLE storage_policies ADD COLUMN b2b_insp_flag boolean DEFAULT false`
- entity_columns: 8개 도메인(11~18)에 rank=53, grid_rank=53, type=boolean으로 추가
- menu_columns: 8개 도메인에 rank=185, grid_rank=175으로 추가 (b2b_wave_group_by와 b2c_wave_flag 사이)
- terminology(B2B 검수 여부 ko/en/ja/zh)는 이미 등록되어 있어 신규 추가 불필요
- entity_columns의 `rank` 컬럼은 NOT NULL이므로 기존 b2b_wave_flag(rank=44) 참고해 53으로 설정

---

## 22:00 — meta-import-mixin.js 에서 템플릿 다운로드 동작하도록 Fix

### 작업 내용
- ServiceUtil.downloadAttachment(attachmentId) 함수 추가
- basic-excel-import-element.js 화면에서 템플릿 다운로드 함수를 ServiceUtil.downloadAttachment(attachmentId)로 변경

### 변경/생성 파일
- `metapage/client/utils/service-util.js` - downloadAttachment(attachmentId) 함수 추가
- `metapage/client/mixin/meta-import-mixin.js` - 템플릿 다운로드 버튼 클릭시 ServiceUtil.downloadAttachment(attachmentId) 호출

---
## 오늘 작업 요약

| 시각 | 작업 | 결과물 |
|------|------|--------|
| 10:00 | BACK_ORDER 재할당 팝업 생성 | `pages/oms/backorder-reallocate-popup.js` |
| 11:00 | StoragePolicy b2b_insp_flag 컬럼 추가 | Entity + DB + entity_columns + menu_columns | 
| 22:00 | meta-import-mixin.js 템플릿 다운로드 기능 Fix |  | 
