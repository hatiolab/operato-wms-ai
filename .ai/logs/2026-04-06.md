# 2026-04-06 작업 로그

## 18:30 — PDA 피킹 화면 목록 모드 개선 (요약카드/필터/스캔 추가)

### 작업 내용
- PDA 피킹 화면(`pda-fulfillment-picking.js`)의 list 모드를 PDA 포장 화면과 동일한 구조로 개선
- 최상단에 대기/진행중/완료 상태별 수량 요약 카드 추가
- 상태별 필터 버튼(필터 칩) 추가
- 하단에 피킹지시번호 바코드 스캔 입력 추가
- 데이터 로딩을 todo+done 병렬 조회로 변경하여 완료 건수도 표시
- 피킹 지시 카드에 완료 상태 배지(초록) 스타일 추가

### 변경/생성 파일
- `frontend/packages/operato-wes/client/pages/pda/pda-fulfillment-picking.js` — summary-cards, filter-chips, scan-task-order 스타일/템플릿/핸들러 추가, _loadTaskList에 done 조회 추가

### 주요 결정 또는 발견 사항
- `ful_trx/picking_tasks/done` API가 이미 존재하여 별도 백엔드 작업 불필요
- `_onScanPickingTask` 핸들러로 바코드 스캔 시 해당 피킹 지시를 바로 선택하여 작업 시작 가능

## 19:00 — PDA 피킹/포장 화면 요약카드 클릭 필터링 + 새로고침 버튼 위치 변경

### 작업 내용
- 요약 카드(대기/진행중/완료) 클릭 시 해당 상태로 리스트 필터링, 재클릭 시 전체 복귀
- filter-chips 섹션 제거 (요약 카드가 필터 역할 대체)
- 새로고침 버튼을 하단 스캔 입력 옆으로 이동 (scan-row flex 레이아웃)
- 피킹/포장 양쪽 화면 모두 동일하게 적용
- filterStatus 초기값을 'TODO' → 'ALL'로 통일

### 변경/생성 파일
- `frontend/packages/operato-wes/client/pages/pda/pda-fulfillment-picking.js` — summary-card에 cursor/active 스타일, _toggleFilter 메서드, scan-row 레이아웃, filter-chips 제거
- `frontend/packages/operato-wes/client/pages/pda/pda-fulfillment-packing.js` — 동일 변경 적용 (summary-card 클릭 필터, filter-chips 제거, 새로고침 이동, _toggleFilter 추가)

### 주요 결정 또는 발견 사항
- filterStatus 값을 'CREATED'/'IN_PROGRESS'/'DONE'/'ALL'로 세분화하여 각 카드별 정확한 필터링 구현
- 동일 카드 재클릭 시 'ALL'로 토글되는 UX 패턴 채택

## 19:15 — PDA 포장 화면 완료 주문 상세 보기 기능 추가

### 작업 내용
- 완료된 포장 지시를 목록에서 클릭하면 complete 모드로 진입하여 포장 결과 상세 표시
- _selectOrder에서 완료 상태 분기 처리: 항목 + 박스 정보 조회 후 complete 모드 전환
- _loadPackingBoxes 메서드 추가: `ful_trx/packing_orders/{id}/boxes` API로 박스유형/운송장번호 등 조회
- _renderCompleteMode를 조회 모드(isViewMode) 지원으로 개선: 소요시간 대신 완료시각 표시, "다음 포장 작업" 버튼 숨김

### 변경/생성 파일
- `frontend/packages/operato-wes/client/pages/pda/pda-fulfillment-packing.js` — _selectOrder 완료 분기, _loadPackingBoxes 추가, _renderCompleteMode isViewMode 처리

### 주요 결정 또는 발견 사항
- PackingOrder 엔티티에는 box_type/invoice_no 필드가 없고, PackingBox 엔티티에 존재 → boxes API 별도 호출 필요
- startedAt === null로 조회 모드(완료 상세 보기)와 작업 완료 모드를 구분

## 19:30 — PDA 출하 화면 도크 선택 시 포장 지시 도크 배정

### 작업 내용
- PDA 출하 화면에서 도크를 선택하면 dock_cd가 NULL인 출하 대기 포장 지시에 선택한 도크를 일괄 배정
- 백엔드: `assignDock(dockCd)` 메서드 추가 — COMPLETED/LABEL_PRINTED/MANIFESTED 상태의 미배정 주문에 도크 배정
- API: `POST /rest/ful_trx/shipping/assign_dock` 엔드포인트 추가
- 프론트: `_onDockChange`에서 `_assignDock` 호출 후 대기 목록 재조회

### 변경/생성 파일
- `src/main/java/operato/wms/fulfillment/service/FulfillmentShippingService.java` — assignDock 메서드 추가
- `src/main/java/operato/wms/fulfillment/rest/FulfillmentTransactionController.java` — POST shipping/assign_dock 엔드포인트 추가
- `frontend/packages/operato-wes/client/pages/pda/pda-fulfillment-shipping.js` — _assignDock 메서드 추가, _onDockChange 수정

### 주요 결정 또는 발견 사항
- 기존 코드에서 packing_orders.dock_cd를 설정하는 곳이 전혀 없었음
- 수동 도크 배정은 PDA 출하 화면에서의 fallback 용도로 유지

## 20:00 — 웨이브 릴리즈 시 택배사/도크 코드 자동 연동

### 작업 내용
- 웨이브 릴리즈(createPickingTasks) 시 웨이브의 carrier_cd로 shipment_orders.carrier_cd 일괄 업데이트
- PackingOrder 생성(createPackingOrders/createPackingOrdersFromBatch) 시 carrier_cd → dock_cd 자동 매핑
- DOCK_CODE 공통코드의 data_1 필드에 carrier_cd를 저장하여 매핑 테이블로 활용
- findDockCdByCarrierCd 유틸리티 메서드 추가

### 변경/생성 파일
- `src/main/java/operato/wms/fulfillment/service/FulfillmentTransactionService.java` — createPickingTasks에 웨이브 carrier_cd 업데이트, createPackingOrders/createPackingOrdersFromBatch에 dock_cd 매핑, findDockCdByCarrierCd 추가

### 주요 결정 또는 발견 사항
- common_code_details.data_1 = carrier_cd, name = dock_cd로 매핑 (스키마 변경 없이 확장 필드 활용)
- ShipmentOrder에는 dock_cd 필드가 없고 PackingOrder에만 존재 → carrier_cd에서 파생

## 20:30 — 피킹 완료 시 작업자(worker_id) 자동 기록

### 작업 내용
- 피킹 작업 완료 시 로그인 사용자의 ID를 picking_tasks.worker_id에 기록
- SecurityUtil.getUser()로 현재 사용자 조회, UPDATE SQL에 worker_id 파라미터 추가

### 변경/생성 파일
- `src/main/java/operato/wms/fulfillment/service/FulfillmentPickingService.java` — completePickingTask에 SecurityUtil/User import 추가, worker_id 업데이트 로직 추가

### 주요 결정 또는 발견 사항
- PickingTask 엔티티에 worker_id 필드와 인덱스(domain_id,worker_id,status)가 이미 존재했으나 값이 설정되지 않고 있었음

## 20:45 — PackingOrder 생성 시 carrier_service_type 복사

### 작업 내용
- PackingOrder 생성 시 ShipmentOrder의 carrier_service_type도 함께 복사하도록 추가
- 개별 피킹(createPackingOrders)과 총량 피킹(createPackingOrdersFromBatch) 모두 적용

### 변경/생성 파일
- `src/main/java/operato/wms/fulfillment/service/FulfillmentTransactionService.java` — createPackingOrders/createPackingOrdersFromBatch에 setCarrierServiceType 추가

### 주요 결정 또는 발견 사항
- PackingOrder 엔티티에 carrier_service_type 필드가 이미 존재(line 165-166)하여 엔티티 수정 불필요
- carrier_cd는 기존 코드에서 이미 복사, dock_cd는 이전 작업에서 매핑 추가 완료

## 21:00 — 포장 완료 시 작업자(worker_id) 자동 기록

### 작업 내용
- 포장 작업 완료(completePackingOrder) 시 로그인 사용자의 ID를 packing_orders.worker_id에 기록
- User.currentUser()로 현재 사용자 조회, UPDATE SQL에 worker_id 파라미터 추가

### 변경/생성 파일
- `src/main/java/operato/wms/fulfillment/service/FulfillmentPackingService.java` — User import 추가, completePackingOrder에 worker_id 업데이트 로직 추가

### 주요 결정 또는 발견 사항
- 피킹의 SecurityUtil.getUser() 패턴과 동일하게 구현하되, 사용자가 User.currentUser()로 수정

---
## 오늘 작업 요약

| 시각 | 작업 | 결과물 |
|------|------|--------|
| 18:30 | PDA 피킹 목록 모드 개선 | `pda-fulfillment-picking.js` — 요약카드/필터/스캔 추가 |
| 19:00 | 피킹/포장 요약카드 클릭 필터 + 새로고침 이동 | `pda-fulfillment-picking.js`, `pda-fulfillment-packing.js` |
| 19:15 | 포장 완료 주문 상세 보기 | `pda-fulfillment-packing.js` — _loadPackingBoxes, isViewMode 처리 |
| 19:30 | PDA 출하 도크 배정 기능 | `FulfillmentShippingService`, `FulfillmentTransactionController`, `pda-fulfillment-shipping.js` |
| 20:00 | 웨이브 릴리즈 택배사/도크 연동 | `FulfillmentTransactionService` — carrier_cd 전파, dock_cd 매핑 |
| 20:30 | 피킹 완료 시 worker_id 기록 | `FulfillmentPickingService` — SecurityUtil으로 사용자 기록 |
| 20:45 | PackingOrder carrier_service_type 복사 | `FulfillmentTransactionService` — setCarrierServiceType 추가 |
| 21:00 | 포장 완료 시 worker_id 기록 | `FulfillmentPackingService` — User.currentUser()로 사용자 기록 |
