# 2026-04-14 작업 로그

## 09:30 — 업로드 팝업에 storageInfoName 프로퍼티 추가

### 작업 내용
- `ox-storage-upload-popup.ts`에 외부에서 스토리지 이름을 지정할 수 있는 `storageInfoName` 프로퍼티 추가
- 팝업 오픈 시 스토리지 목록에서 `storageInfoName`과 이름이 일치하는 항목을 자동 선택
- 미입력 기본값을 `'default'`로 설정 (default 스토리지 자동 선택)
- placeholder 텍스트도 `storageInfoName` 값이 있으면 `-- {storageInfoName} --` 형태로 표시

### 변경/생성 파일
- `frontend/packages/metapage/client/components/popup/ox-storage-upload-popup.ts` — `storageInfoName: string = 'default'` 프로퍼티 추가, `_loadStorages()`에서 이름 기반 자동 선택 로직 반영

### 주요 결정 또는 발견 사항
- `storageInfo: { id, name }` Object 타입 대신 `storageInfoName: string` 으로 변경 — id는 외부에서 알 수 없고 name만 알 수 있기 때문
- 이름 매칭 실패 시 default_flag 스토리지 또는 첫 번째 스토리지로 폴백

---

## 09:40 — 이미지 셀렉터 팝업 업로드 시 storageInfoName 'image' 설정

### 작업 내용
- `ox-storage-image-selector-popup.ts`에서 업로드 팝업 실행 시 `storage-info-name="image"` 속성 추가
- 이미지 업로드 시 'image' 스토리지에 자동으로 저장되도록 유도

### 변경/생성 파일
- `frontend/packages/metapage/client/components/popup/ox-storage-image-selector-popup.ts` — `<ox-storage-upload-popup>`에 `storage-info-name="image"` 추가

### 주요 결정 또는 발견 사항
- 이미지 선택 팝업에서 업로드 시 별도 스토리지 선택 없이 'image' 스토리지를 기본값으로 안내

---
## 오늘 작업 요약

| 시각 | 작업 | 결과물 |
|------|------|--------|
| 09:30 | 업로드 팝업 storageInfoName 프로퍼티 추가 | `ox-storage-upload-popup.ts` |
| 09:40 | 이미지 셀렉터 팝업 업로드 시 storageInfoName 'image' 설정 | `ox-storage-image-selector-popup.ts` |
