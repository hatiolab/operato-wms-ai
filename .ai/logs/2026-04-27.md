# 2026-04-27 작업 로그

## 09:00 — W23-RE-2 git commit

### 작업 내용
- 이전 세션(2026-04-24)에서 구현한 W23-RE-2 작업 내용을 git commit
- 대상: `OmsReplenishOrderService`, `ReplenishOrderController`, `pda-oms-replenish.js`, `route.ts`, `wbs.md`, 로그 파일

### 변경/생성 파일
- 커밋 해시: `28be84c feat: W23-RE-2 보충 작업 PDA 화면 구현 및 to_loc 수동 스캔 지원`

### 주요 결정 또는 발견 사항
- 미커밋 상태였던 W23-RE-2 관련 6개 파일을 하나의 커밋으로 정리

---

## 10:00 — pda-oms-replenish.js list 모드로 전면 개선

### 작업 내용
- `pda-oms-replenish.js`를 `pda-inbound-receiving.js`와 동일한 UX 패턴으로 전면 재설계
- 기존 ready 모드(빈 화면에서 번호 스캔)를 list 모드(현황 요약 + 목록 + 스캔)로 교체
- 화면 흐름: `list` → `work` → `complete` → `list`

### 변경/생성 파일
- `frontend/packages/operato-wes/client/pages/pda/pda-oms-replenish.js` — list 모드 추가 및 구조 전면 재작성

### 주요 결정 또는 발견 사항
- **list 모드 구성**:
  - 상단 현황 카드 3개: 대기(CREATED) / 진행중(IN_PROGRESS) / 완료(COMPLETED) 건수 클릭 시 필터링
  - 중단 보충 지시 카드 목록: 번호·상태 배지·창고·화주사·건수 표시, 진행중 카드는 수량 기반 진행률 바
  - 하단 스캔 입력 + 새로고침 버튼 (항상 고정 표시)
- **work 모드**: 상단 헤더 바(◀ 뒤로가기 + 보충번호) 추가, 나머지 기존 로직 유지
- **완료 후**: complete → "목록으로" 클릭 시 list로 복귀 + 목록 자동 갱신
- `_loadTaskList()`: `order_date = 오늘` + `status IN (CREATED, IN_PROGRESS, COMPLETED)` 조건으로 조회
- 동일 필터 카드 재클릭 시 전체(ALL)로 복귀하는 토글 방식 채택

---

## 11:00 — Week 1 진행 현황 100% 업데이트

### 작업 내용
- `wbs.md` Week 1 진행 현황 테이블에서 W1-F-1·W1-F-2가 미시작으로 남아 있던 것을 수정

### 변경/생성 파일
- `docs/implementation/wbs.md` — 완료 24→26개, 미시작 2→0개, 진행율 92→100%

### 주요 결정 또는 발견 사항
- W1-F-1·W1-F-2는 이전 세션에서 구현 완료(☑)됐으나 현황 테이블만 반영 안 된 상태였음

---

## 11:30 — createB2bPickingTasks → createDirectPickingTasks 범용화 리팩터링

### 작업 내용
- B2B_OUT 업무유형으로 제한되던 직접 피킹 API를 업무유형 무관하게 동작하도록 범용화
- 메서드명·API URL·상수명·훅 키 전체 일괄 rename

### 변경/생성 파일
- `src/.../fulfillment/WmsFulfillmentConstants.java` — `TRX_FUL_*_CREATE_B2B_PICKING` → `TRX_FUL_*_CREATE_DIRECT_PICKING`, 훅 키 rename
- `src/.../fulfillment/service/FulfillmentTransactionService.java` — `createB2bPickingTasks()` → `createDirectPickingTasks()`, B2B_OUT 검증 제거
- `src/.../fulfillment/rest/FulfillmentTransactionController.java` — 메서드명·URL(`b2b_picking/*` → `direct_picking/*`)·주석 일괄 수정

### 주요 결정 또는 발견 사항
- B2B_OUT 검증 제거로 biz_type에 상관없이 ALLOCATED 상태 주문이면 직접 피킹 가능
- API URL 변경(`b2b_picking` → `direct_picking`)은 브레이킹 체인지 — 호출 측 업데이트 필요
- `wave_no = null`이면 포장 지시 자동 생성이 안 된다는 점은 이후 별도 개선 과제로 확인됨

---

## 12:00 — 직접 피킹 검수 설정(RuntimeEnvItem) 추가 및 포장 자동 생성 연결

### 작업 내용
- 웨이브 없이 직접 피킹 완료 시 포장 지시 자동 생성 여부를 창고-화주사 설정으로 제어
- `completePickingTaskWithPacking()`에서 `wave_no = null`이면 `ful.direct_picking.insp_flag` 설정값을 읽어 분기 처리

### 변경/생성 파일
- `src/.../fulfillment/WmsFulfillmentConfigConstants.java` — `DIRECT_PICKING_INSP_FLAG = "ful.direct_picking.insp_flag"` 상수 추가
- `src/.../base/service/RuntimeConfigService.java` — `getRuntimeConfigValue()` null 안전하게 수정 (설정 미등록 시 NPE → null 반환)
- `src/.../fulfillment/service/FulfillmentTransactionService.java` — `RuntimeConfigService` 주입 + `completePickingTaskWithPacking()` wave_no 분기에 else 절 추가

### 주요 결정 또는 발견 사항
- 기존 `completePickingTaskWithPacking()` 로직: `wave_no != null` → 웨이브 `insp_flag` 조회, `wave_no = null` → `inspFlag = false` (포장 미생성)
- 개선 후: `wave_no = null` → `runtime_env_items`에서 `ful.direct_picking.insp_flag` 조회, 미설정 기본값은 `false` (기존 동작 유지)
- `RuntimeConfigService.getRuntimeConfigValue()`가 설정 미등록 시 NPE를 던질 수 있는 잠재 버그를 이번에 같이 수정
- 운영: 창고-화주사별로 `ful.direct_picking.insp_flag = true` 등록하면 직접 피킹 후 포장 지시 자동 생성

---

## 오늘 작업 요약

| 시각  | 작업 | 결과물 |
|-------|------|--------|
| 09:00 | W23-RE-2 git commit | `28be84c` |
| 10:00 | pda-oms-replenish.js list 모드 전면 개선 | `pda-oms-replenish.js` |
| 11:00 | Week 1 진행 현황 100% 업데이트 | `wbs.md` |
| 11:30 | createDirectPickingTasks 범용화 리팩터링 | `WmsFulfillmentConstants.java`, `FulfillmentTransactionService.java`, `FulfillmentTransactionController.java` |
| 12:00 | 직접 피킹 검수 설정 추가 및 포장 자동 생성 연결 | `WmsFulfillmentConfigConstants.java`, `RuntimeConfigService.java`, `FulfillmentTransactionService.java` |
