# 2026-04-04 작업 로그

## 13:00 — PDA 출하 화면 새로고침 버튼 추가

### 작업 내용
- `pda-fulfillment-shipping.js`의 액션 버튼 행에 '새로고침' 버튼 추가
- 초기화 버튼 왼쪽에 배치하여 도크 목록 및 대기 목록을 재조회하는 기능 구현

### 변경/생성 파일
- `frontend/packages/operato-wes/client/pages/pda/pda-fulfillment-shipping.js` — 새로고침 버튼 추가 + `_refresh()` 메서드 추가

### 주요 결정 또는 발견 사항
- 버튼 순서: [도크 선택] ··· [새로고침] [초기화] [전체 확정]
- `_refresh()`는 도크 목록 재조회 + 도크 선택 상태면 대기 목록도 재조회
- `_reset()`은 도크/대기/스캔 전체 리셋 후 도크 목록만 재조회 (기존 로직 유지)

## 13:10 — PDA 포장 화면 검수완료 버튼 추가

### 작업 내용
- `pda-fulfillment-packing.js`의 inspection 모드 헤더에 '검수완료' 버튼 추가
- 이미 서버에서 검수가 완료된 상태의 포장 지시 선택 시, 바코드 스캔 없이 포장 단계로 진행 가능하도록 개선

### 변경/생성 파일
- `frontend/packages/operato-wes/client/pages/pda/pda-fulfillment-packing.js` — '건너뛰기' 옆에 '검수완료' primary 버튼 추가

### 주요 결정 또는 발견 사항
- `completedCount < totalCount` 조건으로 비활성화 → 모든 항목 검수 완료 시에만 활성화
- 클릭 시 기존 `_onInspectionComplete()` 호출하여 packing 모드로 전환
- 사용자가 직접 `?disabled` 조건을 `this.processing`만으로 변경 (completedCount 조건 제거)

## 14:00 — OMS 대시보드 판매채널 통계 공통코드 연동

### 작업 내용
- `OmsDashboardService.getChannelStats()`에서 `cust_cd`를 판매채널 코드로 사용
- 공통코드 `SALES_CHANNEL`에서 채널명을 조회하도록 SQL 수정 (LEFT JOIN common_codes + common_code_details)

### 변경/생성 파일
- `src/main/java/operato/wms/oms/service/OmsDashboardService.java` — getChannelStats SQL에 common_codes JOIN 추가, COALESCE로 채널명 fallback 처리

### 주요 결정 또는 발견 사항
- `COALESCE(ccd.description, so.cust_cd)` 패턴으로 공통코드 미등록 시 원본 코드값 표시
- 응답 키는 `cust_nm`으로 유지 (프론트엔드 호환성)

## 14:20 — OMS 대시보드 '업무 유형별 현황' → '출하 유형별 현황' 변경

### 작업 내용
- `biz_type` 기반 통계를 `ship_type` 기반으로 전환
- 백엔드: `getBizTypeStats()` → `getShipTypeStats()`, 공통코드 `SHIP_TYPE`에서 유형명 조회
- 프론트: 고정 Map → 동적 배열 기반 도넛 차트로 변경

### 변경/생성 파일
- `src/main/java/operato/wms/oms/service/OmsDashboardService.java` — getBizTypeStats → getShipTypeStats (ship_type + SHIP_TYPE 공통코드 JOIN)
- `src/main/java/operato/wms/oms/rest/OmsDashboardController.java` — 엔드포인트 `/biz_type_stats` → `/ship_type_stats`, 반환타입 `List<Map>`
- `frontend/packages/operato-wes/client/pages/oms/oms-home.js` — shipTypeStats 프로퍼티, 동적 레이블/데이터 차트, fetch 메서드 변경

### 주요 결정 또는 발견 사항
- ship_type 값이 가변적이므로 하드코딩된 B2C/B2B 레이블 대신 API 응답 데이터에서 동적으로 labels/data 생성
- Chart.js 색상 팔레트를 6색으로 확장 (NORMAL/RETURN/TRANSFER/SCRAP/EXPORT/ETC 대응)

## 14:40 — OMS 대시보드 '최근 주문 내역' 테이블 컬럼 변경

### 작업 내용
- '업무유형' 컬럼을 '판매채널'로 교체
- 화주사(com_cd), 우선순위(priority_cd), 품목수(total_item), 주문수량(total_order) 컬럼 추가

### 변경/생성 파일
- `frontend/packages/operato-wes/client/pages/oms/oms-home.js` — 테이블 헤더/바디 8컬럼으로 확장

### 주요 결정 또는 발견 사항
- 최종 컬럼 순서: 출하번호 | 화주사 | 판매채널 | 우선순위 | 품목수 | 주문수량 | 상태 | 주문일
- 사용자가 '고객사' → '화주사' 라벨 변경, 화주사를 판매채널 앞으로 이동

## 15:00 — OMS 대시보드 바로가기 버튼 헤더로 이동

### 작업 내용
- 독립 섹션이던 5개 바로가기 버튼(주문 목록, 웨이브 생성, 임포트, 보충 현황, 재고 조회)을 새로고침 버튼 옆 헤더 영역으로 이동
- 스타일을 새로고침 버튼과 동일한 아웃라인 스타일로 통일

### 변경/생성 파일
- `frontend/packages/operato-wes/client/pages/oms/oms-home.js` — `.quick-actions` 섹션 제거, `.header-actions` 영역으로 버튼 통합, CSS 수정

### 주요 결정 또는 발견 사항
- `.header-actions button` 스타일이 기존 `.btn-refresh`와 동일한 아웃라인 디자인 적용
- 반응형 CSS도 함께 업데이트

## 16:00 — shipment-tracking.js 검색 영역 간격 통일

### 작업 내용
- `shipment-tracking.js`의 검색 영역 margin/padding이 다른 목록 화면(`packing-order-list.js`)과 상이하여 통일
- `.page-container`의 `max-width: 1200px; margin: 0 auto;` 제거
- 검색 필드를 flex 고정 width에서 grid 반응형으로 변경

### 변경/생성 파일
- `frontend/packages/operato-wes/client/pages/fulfillment/shipment-tracking.js` — `.search-row`→`.search-form`(grid), `.search-field`→`.form-group`, max-width 제거, focus box-shadow 추가

### 주요 결정 또는 발견 사항
- `packing-order-list.js`를 기준으로 `.search-form`(grid auto-fit), `.form-group`(gap:4px), `padding: 8px 12px`, focus `box-shadow` 패턴으로 통일
- HTML 클래스명도 함께 변경 (`search-row`→`search-form`, `search-field`→`form-group`)

## 16:30 — rwa-home.js page-header 패턴 적용

### 작업 내용
- 반품 대시보드에 `.page-header` 패턴 적용 (oms-home.js 참조)
- 하단 바로가기 버튼들을 헤더 영역으로 이동하여 아웃라인 스타일로 통일
- 섹션 타이틀 이모지 제거, `i18next.t()` 적용

### 변경/생성 파일
- `frontend/packages/operato-wes/client/pages/rwa/rwa-home.js` — `h2`/`[page-description]` 스타일 → `.page-header` 패턴, `.quick-actions` 제거, 버튼 헤더 통합

### 주요 결정 또는 발견 사항
- 새로고침/반품요청/입고처리/검수작업/처분결정 5개 버튼을 `.header-actions`에 배치
- 반응형 `.header-actions { flex-wrap: wrap; }` 추가

## 16:50 — vas-home.js page-header 패턴 적용

### 작업 내용
- VAS 대시보드에 `.page-header` 패턴 적용 (oms-home.js 참조)
- 하단 바로가기 버튼들을 헤더 영역으로 이동하여 아웃라인 스타일로 통일
- 섹션 타이틀 이모지 제거, `i18next.t()` 적용

### 변경/생성 파일
- `frontend/packages/operato-wes/client/pages/vas/vas-home.js` — `h2`/`[page-description]` 스타일 → `.page-header` 패턴, `.quick-actions` 제거, 버튼 헤더 통합

### 주요 결정 또는 발견 사항
- 새로고침/작업 지시 생성/작업 진행 현황/실적 조회/세트 상품 관리 5개 버튼을 `.header-actions`에 배치
- rwa-home.js와 동일한 패턴으로 적용

## 17:10 — fulfillment-home.js 피킹 유형별 차트 데이터 누락 수정

### 작업 내용
- '피킹 유형별 현황' 도넛 차트가 표시되지 않는 문제 수정
- 백엔드 `getPickingStatus()` 메서드에 pick_type별 집계 쿼리 추가

### 변경/생성 파일
- `src/main/java/operato/wms/fulfillment/service/FulfillmentDashboardService.java` — pick_type별 건수(individual, total_pick, zone) 집계 쿼리 추가

### 주요 결정 또는 발견 사항
- 프론트엔드는 `pickingStatus.individual/total_pick/zone` 필드를 참조하는데, 백엔드에서 해당 필드를 반환하지 않아 차트가 비어있었음
- 기존 상태별 집계 쿼리에 추가로 pick_type별 집계 쿼리를 별도 실행하여 결과에 병합

## 17:20 — fulfillment-home.js 피킹 상세 팝업 연동

### 작업 내용
- '최근 피킹 작업' 테이블의 피킹번호 클릭 시 페이지 이동 대신 `picking-task-detail.js` 팝업으로 변경
- 팝업 닫힌 후 대시보드 데이터 자동 새로고침

### 변경/생성 파일
- `frontend/packages/operato-wes/client/pages/fulfillment/fulfillment-home.js` — `import './picking-task-detail'` 추가, `_navigateToPickingDetail()` 메서드를 `UiUtil.openPopupByElement()` 방식으로 변경

### 주요 결정 또는 발견 사항
- `picking-task-detail.js`가 이미 LitElement 기반 팝업 컴포넌트로 설계되어 있어 별도 수정 없이 재사용
- `task-updated` 이벤트 리스너로 대시보드 데이터 자동 갱신

## 17:40 — fulfillment-picking-pc.js page-header 스타일 통일

### 작업 내용
- `picking-task-list.js`의 `.page-header`, `.btn`, `.btn-outline` 스타일 패턴을 `fulfillment-picking-pc.js`에 적용
- 하드코딩 텍스트를 TermsUtil 다국어 함수로 교체

### 변경/생성 파일
- `frontend/packages/operato-wes/client/pages/fulfillment/fulfillment-picking-pc.js` — `.page-header` background/box-shadow 제거, `.btn-icon` → `.btn`+`.btn-outline`, h2/버튼 텍스트 TermsUtil 적용

### 주요 결정 또는 발견 사항
- 2패널 레이아웃 특성상 `padding: 12px 24px`과 `border-bottom` 유지 (일반 페이지의 `margin-bottom` 대신)
- `.page-header h2`에 `var(--title-font)`, `var(--title-text-color)` 테마 변수 적용

## 17:50 — fulfillment-packing-pc.js page-header 스타일 통일

### 작업 내용
- `fulfillment-picking-pc.js`와 동일한 `.page-header` 스타일을 `fulfillment-packing-pc.js`에 적용
- 하드코딩 텍스트를 TermsUtil 다국어 함수로 교체

### 변경/생성 파일
- `frontend/packages/operato-wes/client/pages/fulfillment/fulfillment-packing-pc.js` — `.page-header` background/box-shadow 제거, `.btn-icon` → `.btn`+`.btn-outline`, h2/버튼 텍스트 TermsUtil 적용

### 주요 결정 또는 발견 사항
- fulfillment-picking-pc.js와 완전히 동일한 page-header 패턴 적용 (2패널 레이아웃 전용)
- 피킹/포장 PC 화면 간 스타일 일관성 확보

## 18:00 — shipment-wave-list.js page-header 스타일 통일

### 작업 내용
- `shipment-wave-list.js`의 타이틀/버튼 스타일을 `packing-order-list.js` 패턴에 맞게 수정
- `h2`/`[page-description]` 독립 CSS 제거, `.page-header` + `.header-actions` 패턴 적용
- `.search-actions`, `.btn-primary`, `.btn-secondary`, `.btn-success`, `.action-bar` 스타일 제거

### 변경/생성 파일
- `frontend/packages/operato-wes/client/pages/oms/shipment-wave-list.js` — `.page-header` 패턴 적용, 초기화/조회/자동 웨이브 생성 버튼을 `.header-actions`로 이동, `.btn-outline` 통일

### 주요 결정 또는 발견 사항
- 검색폼 내부의 `.search-actions`와 하단 `.action-bar` 제거 후 헤더 영역으로 통합
- 반응형 `.header-actions { flex-wrap: wrap; }` 추가

## 18:10 — OMS 대시보드 출하번호 클릭 시 상세 팝업 연동

### 작업 내용
- `oms-home.js` '최근 주문 내역' 테이블의 출하번호 클릭 시 페이지 이동 대신 `shipment-order-detail.js` 팝업으로 변경
- 팝업에서 주문 상태 변경 시 대시보드 데이터 자동 새로고침

### 변경/생성 파일
- `frontend/packages/operato-wes/client/pages/oms/oms-home.js` — `import './shipment-order-detail'` 추가, `_navigateToDetail()` 메서드를 `UiUtil.openPopupByElement()` 방식으로 변경

### 주요 결정 또는 발견 사항
- `shipment-order-detail.js`가 이미 LitElement 기반 팝업 컴포넌트로 설계되어 있어 별도 수정 없이 재사용
- `order-updated` 이벤트 리스너로 대시보드 데이터 자동 갱신
- `fulfillment-home.js`의 `picking-task-detail` 팝업 연동과 동일한 패턴 적용

## 18:20 — stock-allocation-list.js page-header 스타일 통일

### 작업 내용
- `stock-allocation-list.js`의 타이틀/버튼 스타일을 표준 `.page-header` 패턴으로 수정
- 독립 `h2` 스타일 제거, `.btn-primary`/`.btn-secondary`/`.search-actions` 스타일 제거

### 변경/생성 파일
- `frontend/packages/operato-wes/client/pages/oms/stock-allocation-list.js` — `h2` 독립 스타일 → `.page-header h2`에 통합, `.header-actions` 추가, 버튼 `btn-outline`으로 변경

### 주요 결정 또는 발견 사항
- `.page-header h2`에 `font: var(--title-font)`, `color: var(--title-text-color)` 테마 변수 적용
- 초기화/조회 버튼을 `.header-actions` div로 래핑하여 일관된 레이아웃 적용

## 18:30 — shipment-order-detail.js 타임라인 시간 정보 표시

### 작업 내용
- 출하 주문 상세 팝업의 상태 타임라인(`_renderTimeline`)에 각 단계의 처리 시간 표시
- `created_at`, `confirmed_at`, `allocated_at`, `released_at`, `shipped_at`, `closed_at` 필드 매핑

### 변경/생성 파일
- `frontend/packages/operato-wes/client/pages/oms/shipment-order-detail.js` — `.timeline-step .time` CSS 추가, `_renderTimeline()` 시간 표시, `_formatShortTime()` 메서드 추가

### 주요 결정 또는 발견 사항
- WAVED/PICKING/PACKING 단계는 엔티티에 별도 시간 필드가 없어 시간 미표시
- `_formatShortTime()` — 타임라인용 간략 포맷 (`MM/DD HH:mm`)
- `.timeline-connector` margin-bottom을 18px → 32px로 증가하여 시간 텍스트 공간 확보

---
## 오늘 작업 요약

| 시각 | 작업 | 결과물 |
|------|------|--------|
| 13:00 | PDA 출하 화면 새로고침 버튼 추가 | `pda-fulfillment-shipping.js` |
| 13:10 | PDA 포장 화면 검수완료 버튼 추가 | `pda-fulfillment-packing.js` |
| 14:00 | OMS 대시보드 판매채널 공통코드 연동 | `OmsDashboardService.java` |
| 14:20 | 출하 유형별 현황으로 변경 | `OmsDashboardService.java`, `OmsDashboardController.java`, `oms-home.js` |
| 14:40 | 최근 주문 내역 테이블 컬럼 변경 | `oms-home.js` |
| 15:00 | 바로가기 버튼 헤더로 이동 | `oms-home.js` |
| 16:00 | shipment-tracking 검색 영역 간격 통일 | `shipment-tracking.js` |
| 16:30 | rwa-home page-header 패턴 적용 | `rwa-home.js` |
| 16:50 | vas-home page-header 패턴 적용 | `vas-home.js` |
| 17:10 | 피킹 유형별 차트 데이터 누락 수정 | `FulfillmentDashboardService.java` |
| 17:20 | 피킹 상세 팝업 연동 | `fulfillment-home.js` |
| 17:40 | fulfillment-picking-pc page-header 통일 | `fulfillment-picking-pc.js` |
| 17:50 | fulfillment-packing-pc page-header 통일 | `fulfillment-packing-pc.js` |
| 18:00 | shipment-wave-list page-header 통일 | `shipment-wave-list.js` |
| 18:10 | OMS 대시보드 출하번호 상세 팝업 연동 | `oms-home.js` |
| 18:20 | stock-allocation-list page-header 통일 | `stock-allocation-list.js` |
| 18:30 | 타임라인 시간 정보 표시 | `shipment-order-detail.js` |
