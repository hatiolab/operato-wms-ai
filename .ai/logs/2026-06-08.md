# 2026-06-08 작업 로그

## 09:00 — fulfillment-packing-pc.js B2B/B2C 포장 화면 분기 처리

### 작업 내용
- B2B 포장 주문 선택 시 운송장 등록 영역 숨김
- B2B 포장 화면에 '거래명세서 출력' 버튼 추가 (출고 확정 버튼 옆)
- B2B는 운송장 없이 출고 확정 가능하도록 `canRelease` 조건 변경
- `_printDeliveryStatement()` 메서드 추가 (`POST ful_trx/packing_orders/{id}/print_statement`)

### 변경/생성 파일
- `frontend/packages/operato-wes/client/pages/fulfillment/fulfillment-packing-pc.js` — B2B/B2C 분기 처리, 거래명세서 출력 버튼 추가

### 주요 결정 또는 발견 사항
- `this.selectedOrder`에 `biz_type` 정보가 없어서 동작하지 않는 문제 발견
- 포장 주문 API 응답에 `biz_type`이 없음 → 백엔드 JOIN 방식(방법 B)으로 해결 결정

---

## 10:00 — PackingOrder.biz_type 필드 추가

### 작업 내용
- `PackingOrder` 엔티티에 `biz_type` 컬럼 추가
- 모든 도메인의 `entity_columns`, `menu_columns`에 `biz_type` 등록

### 변경/생성 파일
- `src/main/java/operato/wms/fulfillment/entity/PackingOrder.java` — `bizType` 필드 + getter/setter 추가
- DB `entity_columns` — 8개 도메인 등록 (rank=55, grid_rank=35)
- DB `menu_columns` — 8개 도메인 등록

---

## 10:30 — 포장 주문 조회 API에 biz_type / cust_nm 포함

### 작업 내용
- `searchTodoPackingOrders`, `searchDonePackingOrders` 두 메서드 모두 `shipment_orders`를 LEFT JOIN하여 `biz_type`, `cust_nm` 응답에 포함
- 프론트엔드가 `order.biz_type`으로 B2B/B2C 분기 가능해짐

### 변경/생성 파일
- `src/main/java/operato/wms/fulfillment/service/FulfillmentPackingService.java` — `searchTodoPackingOrders`, `searchDonePackingOrders` SQL에 JOIN 및 컬럼 추가

---

## 11:00 — 피킹 완료 후 PackingOrder 생성 시 biz_type 복사

### 작업 내용
- 개별 피킹(`createPackingOrders`): `order.getBizType()`을 `packOrder.setBizType()`으로 복사
- 배치/총량 피킹(`createPackingOrdersFromBatch`): 동일하게 각 주문의 `biz_type` 복사

### 변경/생성 파일
- `src/main/java/operato/wms/fulfillment/service/FulfillmentTransactionService.java` — 두 메서드에 `setBizType()` 추가

---

## 11:30 — packing-order-list.js 비지니스 유형 검색 조건 추가

### 작업 내용
- `SHIPMENT_ORDER_BIZ_TYPE` 공통코드를 `ServiceUtil.codeItems()`로 동적 조회
- 검색 폼에 '비지니스 유형' 드롭다운 추가 (상태 다음)
- `_fetchOrders()` 필터에 `biz_type` 반영, `_resetSearch()` 초기화 포함

### 변경/생성 파일
- `frontend/packages/operato-wes/client/pages/fulfillment/packing-order-list.js` — 비지니스 유형 검색 추가

---

## 12:00 — fulfillment-progress.js 비지니스 유형 검색 조건 추가

### 작업 내용
- packing-order-list.js와 동일한 패턴으로 `SHIPMENT_ORDER_BIZ_TYPE` 공통코드 조회
- 검색 폼에 '비지니스 유형' 드롭다운 추가 (웨이브번호 앞)
- `_fetchItems()` 필터 및 `_resetSearch()` 반영

### 변경/생성 파일
- `frontend/packages/operato-wes/client/pages/fulfillment/fulfillment-progress.js` — 비지니스 유형 검색 추가

---

## 12:30 — FulfillmentProgress.biz_type 필드 추가 및 DB View 재생성

### 작업 내용
- `FulfillmentProgress` 엔티티에 `biz_type` 필드 추가
- `fulfillment_progress` View SQL 재생성: `po.biz_type` 컬럼 추가
- `CREATE OR REPLACE VIEW` 오류(컬럼 순서 변경 불가) → `DROP + CREATE` 방식으로 해결

### 변경/생성 파일
- `src/main/java/operato/wms/fulfillment/entity/FulfillmentProgress.java` — `bizType` 필드 + getter/setter, 주석 SQL 업데이트
- DB View `fulfillment_progress` — `po.biz_type` 컬럼 추가 (DROP → CREATE)

### 주요 결정 또는 발견 사항
- `FulfillmentProgress`는 `@Table(ignoreDdl = true)` 뷰 기반 엔티티이므로 DB View를 직접 재생성해야 함
- `CREATE OR REPLACE VIEW`는 컬럼 위치 변경 불가 → DROP 후 재생성 필요

---

## 오늘 작업 요약

| 시각 | 작업 | 결과물 |
|------|------|--------|
| 09:00 | B2B/B2C 포장 화면 분기 | fulfillment-packing-pc.js |
| 10:00 | PackingOrder.biz_type 추가 | PackingOrder.java, entity/menu_columns |
| 10:30 | 포장 주문 API biz_type/cust_nm 포함 | FulfillmentPackingService.java |
| 11:00 | PackingOrder 생성 시 biz_type 복사 | FulfillmentTransactionService.java |
| 11:30 | 포장 주문 목록 비지니스 유형 검색 | packing-order-list.js |
| 12:00 | 풀필먼트 진행 현황 비지니스 유형 검색 | fulfillment-progress.js |
| 12:30 | FulfillmentProgress.biz_type + DB View | FulfillmentProgress.java, fulfillment_progress View |
